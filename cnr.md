# Вопросы для интервью: TypeScript, React, HTTP/HTTPS, REST API, Безопасность

Этот список составлен для подготовки к собеседованиям. Вопросы охватывают TypeScript, React, сетевые протоколы и безопасность.

---

## 🟢 TypeScript: Типизация и сравнение с JavaScript

---

### 1. В чем разница между TypeScript и JavaScript? Какие преимущества даёт TypeScript?

**JavaScript** — динамически типизированный язык. Типы переменных определяются во время выполнения и могут меняться.

**TypeScript** — надмножество JavaScript со статической типизацией. Типы проверяются на этапе компиляции, до выполнения кода.

**Преимущества TypeScript:**

1. **Раннее обнаружение ошибок** — компилятор ловит типовые ошибки до запуска
   ```typescript
   function greet(name: string) {
     return `Hello, ${name.toUpperCase()}`;
   }

   greet(123); // Ошибка компиляции: Argument of type 'number' is not assignable to parameter of type 'string'
   ```

2. **Улучшенный IntelliSense** — IDE понимает структуру данных и предлагает автодополнение
   ```typescript
   interface User {
     id: number;
     name: string;
     email: string;
   }

   function getUser(): User { /* ... */ }
   const user = getUser();
   user. // IDE покажет: id, name, email
   ```

3. **Самодокументирующийся код** — типы служат документацией
   ```typescript
   // Из сигнатуры сразу понятно, что принимает и возвращает функция
   function calculateDiscount(price: number, discountPercent: number): number
   ```

4. **Рефакторинг** — при переименовании поля TypeScript найдёт все использования
5. **Работа в команде** — контракты между модулями явно описаны типами

**Недостатки TypeScript:**
- Дополнительный этап компиляции
- Кривая обучения для новичков
- Иногда избыточная сложность для простых скриптов

**Когда выбрать что:**
- **JavaScript**: прототипы, маленькие скрипты, обучение программированию
- **TypeScript**: командная разработка, большие проекты, долгоживущий код

---

### 2. Что такое строго типизированные и динамически типизированные языки? В чём их различия?

**Динамическая типизация (JavaScript, Python):**
- Типы проверяются во время выполнения
- Переменная может хранить значения разных типов
- Ошибки типов обнаруживаются только при запуске

```javascript
let value = "hello";  // string
value = 42;           // теперь number — допустимо
value = { x: 1 };     // теперь object — тоже допустимо

// Ошибка возникнет только при выполнении
value.toUpperCase();  // TypeError: value.toUpperCase is not a function
```

**Статическая типизация (TypeScript, Java, Go):**
- Типы проверяются во время компиляции
- Тип переменной фиксирован
- Ошибки типов обнаруживаются до запуска

```typescript
let value: string = "hello";
value = 42;  // Ошибка компиляции: Type 'number' is not assignable to type 'string'
```

**Строгая типизация (Strong typing)** — язык не выполняет неявные преобразования типов:

```python
# Python — строго типизирован
"hello" + 5  # TypeError: can only concatenate str to str
```

```javascript
// JavaScript — слабо типизирован
"hello" + 5  // "hello5" — неявное преобразование
```

**TypeScript** — статически и строго типизирован (с оговорками из-за совместимости с JavaScript).

**Сравнение:**

| Характеристика | Динамическая | Статическая |
|----------------|--------------|-------------|
| Когда проверка типов | Рантайм | Компиляция |
| Гибкость | Высокая | Ограниченная |
| Безопасность | Ниже | Выше |
| Скорость разработки | Быстрее для прототипов | Быстрее для больших проектов |
| Производительность | Медленнее (проверки в рантайме) | Быстрее (оптимизации компилятора) |

---

### 3. Зачем в TypeScript нужен тип `any`?

`any` — это «аварийный люк» (escape hatch) TypeScript. Он отключает проверку типов для переменной.

**Когда `any` необходим:**

1. **Миграция с JavaScript** — постепенный перевод большого проекта на TypeScript
   ```typescript
   // Пока не описали типы для legacy-модуля
   const legacyModule: any = require('./legacy-module');
   ```

2. **Работа с динамическими данными неизвестной структуры**
   ```typescript
   // Данные от стороннего API без документации
   const response: any = await fetch('/unknown-api').then(r => r.json());
   ```

3. **Интеграция с библиотеками без типов**
   ```typescript
   declare const thirdPartyLib: any;
   ```

4. **Когда типизация слишком сложна и не оправдывает затраты**

**Проблемы с `any`:**

```typescript
const data: any = getUserData();

data.toUpperCase();     // Нет ошибки компиляции
data.nonExistentMethod(); // Нет ошибки компиляции
const x: number = data;   // Нет ошибки компиляции

// Все эти ошибки проявятся только в рантайме!
```

**Правило:** Избегайте `any` везде, где возможно. Используйте `unknown` или дженерики как более безопасную альтернативу.

---

### 4. В чём разница между `any` и `unknown`?

Оба типа могут принимать любое значение, но ведут себя по-разному при использовании.

**`any` — отключает проверку типов:**

```typescript
let valueAny: any = "hello";

// Всё разрешено — никаких проверок
valueAny.foo.bar.baz;     // OK (рантайм ошибка)
valueAny.toUpperCase();   // OK
valueAny();               // OK (рантайм ошибка, если не функция)
const num: number = valueAny; // OK
```

**`unknown` — требует проверки типа перед использованием:**

```typescript
let valueUnknown: unknown = "hello";

// Ничего нельзя делать без проверки типа
valueUnknown.toUpperCase();   // Ошибка: Object is of type 'unknown'
const num: number = valueUnknown; // Ошибка: Type 'unknown' is not assignable to type 'number'

// Нужна проверка типа (type narrowing)
if (typeof valueUnknown === "string") {
  valueUnknown.toUpperCase(); // OK — TypeScript знает, что это string
}

// Или type assertion (менее безопасно)
const str = valueUnknown as string;
str.toUpperCase(); // OK
```

**Сравнение:**

| Характеристика | `any` | `unknown` |
|----------------|-------|-----------|
| Присвоить любое значение | ✅ | ✅ |
| Использовать без проверки | ✅ | ❌ |
| Присвоить другому типу | ✅ | ❌ (только после проверки) |
| Вызвать методы | ✅ | ❌ (только после проверки) |
| Type-safe | ❌ | ✅ |

**Когда что использовать:**
- `unknown` — для данных из внешних источников (API, пользовательский ввод), когда хотите сохранить типобезопасность
- `any` — для быстрой миграции с JavaScript, временных решений

**Практический пример:**

```typescript
// Обработка ошибок
function handleError(error: unknown) {
  if (error instanceof Error) {
    console.log(error.message); // OK
  } else if (typeof error === "string") {
    console.log(error); // OK
  } else {
    console.log("Unknown error");
  }
}

// С any ошибка может проскочить
function handleErrorUnsafe(error: any) {
  console.log(error.message); // Рантайм ошибка, если error не объект
}
```

---

## 🟡 React: Основы и Популярность

---

### 5. Почему React стал таким популярным? Какие преимущества его использования?

**Причины популярности React:**

1. **Компонентный подход** — UI разбивается на независимые, переиспользуемые части
   ```jsx
   // Компонент — изолированный блок с логикой и разметкой
   function UserCard({ user }) {
     return (
       <div className="card">
         <Avatar src={user.avatar} />
         <h3>{user.name}</h3>
       </div>
     );
   }
   ```

2. **Декларативность** — описываем «что хотим увидеть», а не «как этого достичь»
   ```jsx
   // Декларативно (React)
   function Counter({ count }) {
     return <span>{count}</span>;
   }

   // Императивно (vanilla JS)
   function updateCounter(count) {
     document.getElementById('counter').textContent = count;
   }
   ```

3. **Virtual DOM** — эффективное обновление UI с минимальными операциями над реальным DOM

4. **Односторонний поток данных** — предсказуемое поведение, легче отлаживать
   ```
   State → View → Action → State (новый) → View (обновленный)
   ```

5. **Огромная экосистема** — решения для любых задач (роутинг, стейт-менеджмент, формы, тестирование)

6. **Поддержка Facebook/Meta** — стабильность, активная разработка

7. **JSX** — HTML-подобный синтаксис в JavaScript, интуитивно понятный

8. **React Native** — тот же подход для мобильной разработки

**Преимущества для разработчиков:**
- Большое комьюнити и обилие ресурсов для обучения
- Востребованность на рынке труда
- Гибкость — React не навязывает архитектуру
- Хорошие DevTools для отладки

**Преимущества для бизнеса:**
- Быстрая разработка благодаря переиспользуемым компонентам
- Легко найти разработчиков
- Код легче поддерживать

**Потенциальные недостатки:**
- Только View-слой — нужно выбирать дополнительные инструменты
- Слишком много свободы может привести к хаотичной архитектуре
- Частые изменения (хотя React стабилизировался)

---

## 🔴 HTTP/HTTPS и Безопасность

---

### 6. Что такое HTTP и HTTPS? В чём их различия?

**HTTP (HyperText Transfer Protocol)** — протокол передачи данных в вебе. Работает поверх TCP, используется для запросов между клиентом и сервером.

**Как работает HTTP:**
```
Клиент                          Сервер
   |---- GET /page.html ---------->|
   |<--- 200 OK + HTML контент ----|
```

Данные передаются **открытым текстом** — любой, кто перехватит трафик, увидит содержимое.

**HTTPS (HTTP Secure)** — HTTP с шифрованием через TLS/SSL. Данные зашифрованы, перехват бесполезен.

```
Клиент                          Сервер
   |---- TLS Handshake ----------->|
   |<--- Сертификат + ключ --------|
   |---- Зашифрованный запрос ---->|
   |<--- Зашифрованный ответ ------|
```

**Сравнение:**

| Характеристика | HTTP | HTTPS |
|----------------|------|-------|
| Порт по умолчанию | 80 | 443 |
| Шифрование | ❌ Нет | ✅ TLS/SSL |
| Защита от перехвата | ❌ | ✅ |
| Подтверждение подлинности сервера | ❌ | ✅ (через сертификат) |
| SEO | Хуже | Лучше (Google предпочитает HTTPS) |
| Скорость | Чуть быстрее | Небольшой overhead на шифрование |

**Что даёт HTTPS:**

1. **Конфиденциальность** — данные зашифрованы, нельзя прочитать
2. **Целостность** — данные нельзя изменить при передаче
3. **Аутентификация** — подтверждение, что вы общаетесь с настоящим сервером

**Когда HTTPS критически важен:**
- Формы авторизации (логин/пароль)
- Платёжные данные
- Персональные данные
- Любые современные веб-приложения (HTTP устарел)

---

### 7. Что такое SSL/TLS сертификаты? Зачем нужны Certificate Authorities (CA)?

**SSL/TLS сертификат** — электронный документ, который:
1. Подтверждает идентичность владельца домена
2. Содержит публичный ключ для шифрования

**Как работает:**

```
1. Браузер запрашивает HTTPS-соединение
2. Сервер отправляет свой сертификат
3. Браузер проверяет:
   - Сертификат не просрочен
   - Выдан доверенным CA
   - Домен совпадает
4. Браузер использует публичный ключ для начала защищённого соединения
```

**Certificate Authority (CA)** — доверенная организация, которая выдаёт и подписывает сертификаты.

**Зачем нужны CA:**

Проблема: Как браузер узнает, что сертификат настоящий, а не созданный злоумышленником?

Решение: **Цепочка доверия (Chain of Trust)**

```
Root CA (встроен в браузер/ОС)
    ↓ подписывает
Intermediate CA
    ↓ подписывает
Сертификат сайта example.com
```

- Корневые CA предустановлены в браузерах и операционных системах
- Браузер доверяет сертификатам, подписанным известными CA
- CA проверяет владельца домена перед выдачей сертификата

**Типы валидации сертификатов:**

| Тип | Что проверяется | Время | Для кого |
|-----|-----------------|-------|----------|
| DV (Domain Validation) | Контроль над доменом | Минуты | Блоги, небольшие сайты |
| OV (Organization Validation) | + Существование организации | Дни | Компании |
| EV (Extended Validation) | + Юридическая проверка | Недели | Банки, финансы |

**Популярные CA:** Let's Encrypt (бесплатный), DigiCert, Comodo, GlobalSign.

---

### 8. Что такое self-signed сертификат? Чем он отличается от сертификата от CA?

**Self-signed (самоподписанный) сертификат** — сертификат, подписанный его же создателем, а не доверенным CA.

```bash
# Создание self-signed сертификата
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365
```

**Сравнение:**

| Характеристика | Self-signed | CA-signed |
|----------------|-------------|-----------|
| Стоимость | Бесплатно | Бесплатно (Let's Encrypt) — платно |
| Доверие браузеров | ❌ Предупреждение | ✅ Доверяют автоматически |
| Шифрование | ✅ Работает | ✅ Работает |
| Подтверждение идентичности | ❌ Нет | ✅ Да |
| Подходит для production | ❌ | ✅ |

**Предупреждение браузера для self-signed:**
```
Your connection is not private
Attackers might be trying to steal your information from example.com
NET::ERR_CERT_AUTHORITY_INVALID
```

**Когда self-signed подходит:**
- Локальная разработка (`localhost`)
- Внутренние серверы в закрытой сети
- Тестовые окружения
- Когда вы контролируете все клиенты (можете добавить сертификат в доверенные)

**Когда нужен CA-signed:**
- Публичные веб-сайты
- Любые приложения с пользователями
- API для внешних клиентов
- Production-окружения

---

### 9. Какие уязвимости возникают при использовании self-signed сертификата? Риски для сайта и пользователя.

**Для пользователя:**

1. **Уязвимость к MITM (Man-in-the-Middle)**

   Без доверенного CA пользователь не может проверить, что сервер настоящий:
   ```
   Пользователь <---> [Злоумышленник] <---> Настоящий сервер
                      (свой self-signed)
   ```

   Злоумышленник может:
   - Перехватывать все данные (пароли, платежи)
   - Модифицировать контент
   - Внедрять вредоносный код

2. **Привычка игнорировать предупреждения**

   Если пользователи привыкли кликать «Продолжить» на предупреждениях, они сделают это и на вредоносном сайте.

3. **Отсутствие гарантий идентичности**

   Self-signed сертификат может создать кто угодно для любого домена.

**Для сайта/бизнеса:**

1. **Потеря доверия пользователей**
   - Предупреждения браузера отпугивают посетителей
   - Выглядит непрофессионально и подозрительно

2. **SEO-проблемы**
   - Google понижает сайты без валидного HTTPS

3. **Несовместимость**
   - Некоторые браузеры и приложения полностью блокируют self-signed
   - API-клиенты требуют дополнительной настройки для игнорирования ошибок

4. **Compliance-нарушения**
   - PCI DSS и другие стандарты требуют валидные сертификаты

**Почему шифрования недостаточно:**

Self-signed сертификат шифрует соединение, но не решает вопрос **аутентификации**:
- Шифрование без аутентификации = вы надёжно общаетесь с кем-то, но не знаете с кем
- Это может быть злоумышленник

---

### 10. Как работает атака Man-in-the-Middle (MITM) на практике?

**MITM** — атака, при которой злоумышленник встраивается между клиентом и сервером, перехватывая и/или модифицируя трафик.

**Сценарий атаки:**

```
1. Обычное соединение:
   Пользователь <-----> Сервер банка

2. MITM:
   Пользователь <---> Злоумышленник <---> Сервер банка
                      (перехват)
```

**Практические способы реализации:**

1. **Атака на Wi-Fi (Evil Twin)**
   ```
   - Злоумышленник создаёт точку доступа "Free_Airport_WiFi"
   - Пользователь подключается
   - Весь трафик идёт через злоумышленника
   ```

2. **ARP Spoofing (в локальной сети)**
   ```
   - Злоумышленник отправляет ложные ARP-пакеты
   - Трафик жертвы перенаправляется через его машину
   ```

3. **DNS Spoofing**
   ```
   - Подмена DNS-ответа
   - bank.com → IP злоумышленника вместо настоящего
   ```

4. **SSL Stripping**
   ```
   - Понижение HTTPS до HTTP
   - Пользователь думает, что на HTTPS, а трафик открытый
   ```

**Что может делать атакующий:**

```
Клиент                    MITM                     Сервер
   |--- Логин: admin ----->|                          |
   |                       |--- Логин: admin -------->|
   |                       |<-- Успешный вход --------|
   |<-- "Сессия истекла" --|                          |

   # Атакующий получил сессию, пользователь — ошибку
```

- **Перехват данных:** пароли, cookies, платёжные данные
- **Модификация:** изменение суммы перевода, подмена контента
- **Инъекция:** добавление вредоносного JavaScript

**Как HTTPS защищает от MITM:**

```
С HTTPS:
Клиент <--[шифрование]--> Сервер

MITM:
   |------ Зашифрованный трафик ------|
   Злоумышленник видит: aN9x$#mK2... (бесполезно)
```

При попытке подменить сертификат:
- Браузер обнаружит, что сертификат не от доверенного CA
- Пользователь увидит предупреждение
- Соединение не установится (при HSTS)

**Защита:**
- Использовать HTTPS с валидным сертификатом
- HSTS (HTTP Strict Transport Security) — запрет HTTP
- Certificate Pinning для критичных приложений
- Не игнорировать предупреждения браузера
- Не подключаться к неизвестным Wi-Fi сетям

---

## 🟣 REST API

---

### 11. Что такое REST API?

**REST (Representational State Transfer)** — архитектурный стиль для построения веб-сервисов. **REST API** — интерфейс, следующий принципам REST.

**Ключевые принципы REST:**

1. **Клиент-сервер** — разделение ответственности
2. **Stateless (без состояния)** — каждый запрос содержит всю необходимую информацию
3. **Кэшируемость** — ответы могут кэшироваться
4. **Единообразный интерфейс** — стандартные HTTP-методы и URL-структура
5. **Многоуровневая система** — клиент не знает, общается ли с сервером напрямую или через прокси

**HTTP-методы и их семантика:**

| Метод | Действие | Идемпотентность | Пример |
|-------|----------|-----------------|--------|
| GET | Получение | Да | `GET /users/123` |
| POST | Создание | Нет | `POST /users` |
| PUT | Полное обновление | Да | `PUT /users/123` |
| PATCH | Частичное обновление | Да | `PATCH /users/123` |
| DELETE | Удаление | Да | `DELETE /users/123` |

**Идемпотентность** — повторный вызов даёт тот же результат (GET, PUT, DELETE).

**Пример REST API:**

```
# Получить список пользователей
GET /api/users
Response: [{ "id": 1, "name": "John" }, ...]

# Получить конкретного пользователя
GET /api/users/1
Response: { "id": 1, "name": "John", "email": "john@example.com" }

# Создать пользователя
POST /api/users
Body: { "name": "Jane", "email": "jane@example.com" }
Response: { "id": 2, "name": "Jane", "email": "jane@example.com" }

# Обновить пользователя
PUT /api/users/2
Body: { "name": "Jane Doe", "email": "jane@example.com" }

# Удалить пользователя
DELETE /api/users/2
Response: 204 No Content
```

**HTTP-коды ответов:**

| Код | Значение | Когда использовать |
|-----|----------|-------------------|
| 200 | OK | Успешный GET, PUT, PATCH |
| 201 | Created | Успешный POST (ресурс создан) |
| 204 | No Content | Успешный DELETE |
| 400 | Bad Request | Невалидные данные |
| 401 | Unauthorized | Требуется аутентификация |
| 403 | Forbidden | Нет прав доступа |
| 404 | Not Found | Ресурс не найден |
| 500 | Server Error | Ошибка сервера |

---

### 12. Почему REST API так распространён по сравнению с альтернативами?

**Преимущества REST:**

1. **Простота** — использует стандартный HTTP, знакомый всем разработчикам
2. **Универсальность** — любой HTTP-клиент может работать с REST API
3. **Масштабируемость** — stateless позволяет легко горизонтально масштабировать
4. **Кэширование** — встроенная поддержка через HTTP-заголовки
5. **Инструментарий** — обилие библиотек, инструментов тестирования, документации

**Альтернативы и их особенности:**

**GraphQL:**
```graphql
# Клиент запрашивает только нужные поля
query {
  user(id: 1) {
    name
    posts {
      title
    }
  }
}
```
- ✅ Гибкие запросы, нет over-fetching
- ✅ Один endpoint
- ❌ Сложнее кэширование
- ❌ Круче кривая обучения
- **Когда выбрать:** сложные связанные данные, мобильные приложения (экономия трафика)

**gRPC:**
```protobuf
service UserService {
  rpc GetUser (UserRequest) returns (User);
}
```
- ✅ Высокая производительность (Protocol Buffers)
- ✅ Строгая типизация
- ✅ Двунаправленный streaming
- ❌ Не работает напрямую в браузере
- ❌ Сложнее отлаживать (бинарный протокол)
- **Когда выбрать:** микросервисы, внутренние API, высокие нагрузки

**SOAP:**
- ✅ Стандартизирован (WSDL)
- ❌ Многословный XML
- ❌ Сложный
- **Когда выбрать:** enterprise-интеграции, legacy-системы

**Почему REST побеждает в большинстве случаев:**
- Достаточно хорош для 90% задач
- Минимальный порог входа
- Легко документировать (OpenAPI/Swagger)
- Работает везде — браузеры, мобильные, IoT
- Огромное комьюнити и готовые решения

---

### 13. Как бы выглядел REST API для управления EC2-инстансами?

**Пример дизайна REST API для EC2:**

```
# Базовый URL
https://api.cloud.example.com/v1

# Ресурс: instances
```

**Endpoints:**

```bash
# Список всех инстансов
GET /instances
Response: {
  "instances": [
    { "id": "i-123", "type": "t2.micro", "state": "running", ... },
    { "id": "i-456", "type": "t2.small", "state": "stopped", ... }
  ]
}

# Получить конкретный инстанс
GET /instances/i-123

# Создать новый инстанс
POST /instances
Body: { "type": "t2.micro", "ami_id": "ami-12345", "tags": { "Name": "NewServer" } }
Response: 201 Created

# Обновить инстанс
PATCH /instances/i-123
Body: { "type": "t2.small" }

# Удалить (terminate) инстанс
DELETE /instances/i-123
Response: 204 No Content

# Действия над инстансом (actions как sub-resource)
POST /instances/i-123/actions
Body: { "action": "start" }  # или "stop", "reboot"
```

**Фильтрация и пагинация:**

```bash
GET /instances?state=running&type=t2.micro
GET /instances?limit=20&next_token=abc123
```

---

### 14. Что такое API Versioning и зачем оно нужно?

**API Versioning** — практика поддержки нескольких версий API одновременно для обеспечения обратной совместимости.

**Зачем нужно:**

```
Версия 1.0                    Версия 2.0
-----------                   -----------
GET /users                    GET /users
Response:                     Response:
{                             {
  "name": "John Doe"            "first_name": "John",
}                               "last_name": "Doe"
                              }
```

Без версионирования обновление API сломает всех клиентов, использующих старый формат.

**Способы версионирования:**

1. **URL Path (самый распространённый)**
   ```
   GET /v1/users
   GET /v2/users
   ```
   - ✅ Явно и понятно
   - ✅ Легко кэшировать
   - ❌ «Не RESTful» (версия — не ресурс)

2. **Query Parameter**
   ```
   GET /users?version=1
   GET /users?version=2
   ```
   - ✅ Просто добавить
   - ❌ Легко забыть указать
   - ❌ Сложнее кэшировать

3. **Header**
   ```
   GET /users
   Accept: application/vnd.myapi.v1+json

   # или кастомный заголовок
   X-API-Version: 2
   ```
   - ✅ URL остаётся чистым
   - ❌ Менее заметно
   - ❌ Сложнее тестировать в браузере

4. **Content Negotiation**
   ```
   Accept: application/vnd.myapi+json; version=2
   ```

**Стратегии поддержки версий:**

```
Timeline:
  v1 (deprecated) ←── ещё работает, но не развивается
  v2 (current)    ←── основная версия
  v3 (beta)       ←── для early adopters
```

**Правила хорошего версионирования:**
- Документируйте breaking changes
- Давайте время на миграцию (deprecation period)
- Семантическое версионирование (MAJOR.MINOR.PATCH)
- Уведомляйте пользователей о deprecation

---

### 15. Как изменить endpoint без API versioning?

Если версионирование не используется, есть несколько подходов для безболезненных изменений:

**1. Аддитивные изменения (добавление полей)**

```json
// До
{ "name": "John" }

// После — добавляем новые поля, не удаляя старые
{ "name": "John", "first_name": "John", "last_name": "Doe" }
```
- Клиенты игнорируют неизвестные поля
- Постепенно переходят на новые поля
- Через время deprecated поля удаляются

**2. Deprecation Headers**

```http
HTTP/1.1 200 OK
Deprecation: Sun, 01 Jun 2025 00:00:00 GMT
Sunset: Sun, 01 Dec 2025 00:00:00 GMT
Link: </v2/users>; rel="successor-version"
```

**3. Feature Flags / Параметры**

```bash
# Старое поведение (по умолчанию)
GET /users

# Новое поведение по запросу
GET /users?format=extended
```

**4. Разные представления одного ресурса**

```bash
# Краткое представление
GET /users/123
{ "id": 123, "name": "John" }

# Полное представление
GET /users/123?expand=profile,settings
{ "id": 123, "name": "John", "profile": {...}, "settings": {...} }
```

**5. HATEOAS (Hypermedia)**

```json
{
  "name": "John",
  "_links": {
    "self": { "href": "/users/123" },
    "profile": { "href": "/users/123/profile" },
    "orders": { "href": "/users/123/orders" }
  }
}
```
Клиенты следуют ссылкам, а не хардкодят URL-ы. Изменение структуры ссылок не ломает клиентов.

**6. Content Negotiation**

```http
# Клиент указывает, какой формат понимает
Accept: application/vnd.myapi+json; profile="extended"
```

**Ограничения без версионирования:**
- Нельзя удалять поля
- Нельзя менять типы данных
- Нельзя менять семантику существующих полей
- Сложно делать крупные изменения

**Рекомендация:** Для production API всё же лучше использовать версионирование с самого начала — это даёт больше гибкости.

---

## Итоги

### TypeScript
- Статическая типизация помогает ловить ошибки до runtime
- `any` отключает проверки, `unknown` требует проверки перед использованием
- TypeScript особенно полезен в больших проектах и командах

### React
- Популярен благодаря компонентному подходу, декларативности и экосистеме
- Virtual DOM и односторонний поток данных упрощают разработку и отладку

### HTTP/HTTPS и Безопасность
- HTTPS = HTTP + шифрование TLS
- CA-сертификаты обеспечивают доверие через цепочку подписей
- Self-signed сертификаты — только для разработки
- HTTPS защищает от MITM-атак

### REST API
- Простой, универсальный, хорошо масштабируется
- Используйте правильные HTTP-методы и коды ответов
- Версионирование API — важная практика для поддержки обратной совместимости
