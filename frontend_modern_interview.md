# Современные вопросы Frontend + React (Modern & Advanced)

Этот файл дополняет базовые списки вопросами по современному стеку, инструментам и архитектуре. Вопросы разделены на **Базовые** (обязательно для Middle) и **Продвинутые** (ожидается от уверенного Middle/Senior).

## 1. TypeScript

### 🟢 Базовые

1.  **Interface vs Type**: В чем разница? Когда лучше использовать одно, а когда другое? (Расширение интерфейсов vs Intersection типов).

    **Ответ:**

    `interface` и `type` во многом похожи, но имеют важные различия:

    **Interface:**
    - Предназначен для описания структуры объектов
    - Поддерживает **declaration merging** (можно объявить один интерфейс несколько раз, и они объединятся)
    - Расширяется через `extends`
    ```typescript
    interface User {
      name: string;
    }
    interface User {
      age: number;
    }
    // Результат: { name: string; age: number }
    ```

    **Type:**
    - Более универсальный — может описывать примитивы, union-типы, tuple
    - Не поддерживает declaration merging
    - Комбинируется через `&` (intersection)
    ```typescript
    type ID = string | number;
    type UserWithRole = User & { role: string };
    ```

    **Когда что использовать:**
    - `interface` — для описания объектов, классов, публичных API (библиотек)
    - `type` — для union-типов, сложных композиций, когда нужна гибкость
2.  **Базовые типы**: `any`, `unknown`, `void`, `never`. Почему `unknown` безопаснее `any`?

    **Ответ:**

    **`any`** — отключает проверку типов. Переменной с `any` можно присвоить что угодно и использовать как угодно. TypeScript не будет ругаться, но вы теряете всю типобезопасность.

    **`unknown`** — "безопасный any". Тоже может хранить любое значение, но **использовать его нельзя без проверки типа**:
    ```typescript
    let value: unknown = "hello";
    // value.toUpperCase(); // Ошибка!
    if (typeof value === "string") {
      value.toUpperCase(); // OK после проверки
    }
    ```

    **`void`** — отсутствие возвращаемого значения. Используется для функций, которые ничего не возвращают:
    ```typescript
    function logMessage(msg: string): void {
      console.log(msg);
    }
    ```

    **`never`** — тип, который никогда не наступит. Используется для:
    - Функций, которые всегда выбрасывают ошибку
    - Функций с бесконечным циклом
    - Exhaustive checks в switch/if
    ```typescript
    function fail(msg: string): never {
      throw new Error(msg);
    }
    ```

    **Почему `unknown` безопаснее:** `any` позволяет делать что угодно без проверок, а `unknown` заставляет сначала проверить тип — это предотвращает runtime-ошибки.
3.  **Optional chaining & Nullish coalescing**: Как работают `?.` и `??`. Чем `??` отличается от `||`?

    **Ответ:**

    **Optional chaining (`?.`)** — безопасный доступ к вложенным свойствам. Если значение слева `null` или `undefined`, выражение вернёт `undefined` вместо ошибки:
    ```typescript
    const user = { address: { city: "Moscow" } };
    console.log(user?.address?.city);    // "Moscow"
    console.log(user?.contacts?.phone);  // undefined (без ошибки)

    // Работает с методами и массивами:
    obj?.method?.();
    arr?.[0];
    ```

    **Nullish coalescing (`??`)** — возвращает правый операнд, если левый `null` или `undefined`:
    ```typescript
    const value = null ?? "default";  // "default"
    const count = 0 ?? 10;            // 0
    ```

    **Отличие `??` от `||`:**
    - `||` считает "ложными" значения: `false`, `0`, `""`, `null`, `undefined`, `NaN`
    - `??` считает "ложными" только `null` и `undefined`

    ```typescript
    // Важная разница:
    const count = 0;
    console.log(count || 10);  // 10 (0 считается falsy)
    console.log(count ?? 10);  // 0  (0 — валидное значение)

    const name = "";
    console.log(name || "Anonymous");  // "Anonymous"
    console.log(name ?? "Anonymous");  // "" (пустая строка сохраняется)
    ```

    **Вывод:** используйте `??` когда `0`, `""` или `false` — это валидные значения.
4.  **Enums**: Зачем нужны и какие проблемы могут создать (размер бандла)? Альтернативы (const object).

    **Ответ:**

    **Enum** — способ определить набор именованных констант:
    ```typescript
    enum Status {
      Pending,    // 0
      Active,     // 1
      Completed   // 2
    }

    enum Direction {
      Up = "UP",
      Down = "DOWN"
    }
    ```

    **Зачем нужны:**
    - Группируют связанные константы
    - Обеспечивают автодополнение в IDE
    - Делают код более читаемым (`Status.Active` вместо магического числа `1`)

    **Проблемы:**
    1. **Размер бандла** — обычные enum компилируются в объект с IIFE:
    ```javascript
    // TypeScript enum превращается в:
    var Status;
    (function (Status) {
      Status[Status["Pending"] = 0] = "Pending";
      // ... много кода
    })(Status || (Status = {}));
    ```

    2. **Числовые enum** позволяют присваивать любые числа — это не безопасно

    **Альтернативы:**

    1. **`const enum`** — полностью инлайнится, не создаёт runtime-объект:
    ```typescript
    const enum Status { Pending, Active }
    // Status.Active → просто 1 в коде
    ```

    2. **`as const` объект** (рекомендуемый подход):
    ```typescript
    const Status = {
      Pending: "pending",
      Active: "active",
    } as const;

    type Status = typeof Status[keyof typeof Status];
    // "pending" | "active"
    ```

    **Вывод:** для нового кода предпочтительнее `as const` объекты — они легче, tree-shakeable и более предсказуемы.

### 🟡 Продвинутые

5.  **Generics (Обобщения)**: Как написать функцию, которая принимает аргумент любого типа и возвращает его же? (`<T>`). Зачем нужны ограничения (`extends`)?

    **Ответ:**

    **Generics** позволяют создавать переиспользуемый код, который работает с разными типами, сохраняя типобезопасность.

    **Базовый пример — функция identity:**
    ```typescript
    // Без generics — теряем информацию о типе:
    function identity(arg: any): any {
      return arg;
    }

    // С generics — тип сохраняется:
    function identity<T>(arg: T): T {
      return arg;
    }

    const num = identity(42);        // тип: number
    const str = identity("hello");   // тип: string
    ```

    **Зачем нужны ограничения (`extends`):**

    Иногда нужно ограничить, какие типы принимает generic. Например, если функция работает со свойством `length`:
    ```typescript
    // Ошибка: T может не иметь length
    function getLength<T>(arg: T): number {
      return arg.length; // Property 'length' does not exist
    }

    // Решение — ограничение:
    interface HasLength {
      length: number;
    }

    function getLength<T extends HasLength>(arg: T): number {
      return arg.length; // OK
    }

    getLength("hello");     // OK — строка имеет length
    getLength([1, 2, 3]);   // OK — массив имеет length
    getLength(123);         // Ошибка — число не имеет length
    ```

    **Практические примеры:**
    ```typescript
    // Generic для получения свойства объекта
    function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
      return obj[key];
    }

    // Generic компонент React
    interface ListProps<T> {
      items: T[];
      renderItem: (item: T) => React.ReactNode;
    }
    ```
6.  **Utility Types**: Как работают `Partial<T>`, `Pick<T, K>`, `Omit<T, K>`, `Record<K, T>`?

    **Ответ:**

    Utility Types — встроенные в TypeScript типы-помощники для трансформации других типов.

    **Исходный тип для примеров:**
    ```typescript
    interface User {
      id: number;
      name: string;
      email: string;
      age: number;
    }
    ```

    **`Partial<T>`** — делает все свойства опциональными:
    ```typescript
    type PartialUser = Partial<User>;
    // { id?: number; name?: string; email?: string; age?: number }

    // Полезно для функций обновления:
    function updateUser(id: number, updates: Partial<User>) {
      // можно передать только изменённые поля
    }
    updateUser(1, { name: "New Name" }); // OK
    ```

    **`Pick<T, K>`** — выбирает только указанные свойства:
    ```typescript
    type UserPreview = Pick<User, "id" | "name">;
    // { id: number; name: string }
    ```

    **`Omit<T, K>`** — исключает указанные свойства (противоположность Pick):
    ```typescript
    type UserWithoutEmail = Omit<User, "email">;
    // { id: number; name: string; age: number }

    // Полезно для создания DTO без служебных полей:
    type CreateUserDTO = Omit<User, "id">;
    ```

    **`Record<K, T>`** — создаёт объектный тип с ключами K и значениями T:
    ```typescript
    type UserRoles = Record<string, boolean>;
    // { [key: string]: boolean }

    type StatusMap = Record<"pending" | "active" | "done", User[]>;
    // { pending: User[]; active: User[]; done: User[] }

    // Практический пример — словарь по ID:
    type UsersById = Record<number, User>;
    ```

    **Комбинирование:**
    ```typescript
    // Форма редактирования — все поля опциональны, кроме id
    type EditUserForm = Partial<Omit<User, "id">> & Pick<User, "id">;
    ```
7.  **Type Guards (Защитники типа)**: Как работает оператор `is`? Разница между `typeof` и `instanceof`.

    **Ответ:**

    **Type Guards** — это способы сузить тип переменной внутри условного блока.

    **`typeof`** — проверяет примитивные типы:
    ```typescript
    function process(value: string | number) {
      if (typeof value === "string") {
        // TypeScript знает, что здесь value — string
        return value.toUpperCase();
      }
      // Здесь value — number
      return value.toFixed(2);
    }
    ```
    Работает только с: `"string"`, `"number"`, `"boolean"`, `"symbol"`, `"undefined"`, `"object"`, `"function"`, `"bigint"`

    **`instanceof`** — проверяет, является ли объект экземпляром класса:
    ```typescript
    class Dog { bark() {} }
    class Cat { meow() {} }

    function makeSound(animal: Dog | Cat) {
      if (animal instanceof Dog) {
        animal.bark();  // OK
      } else {
        animal.meow();  // OK
      }
    }
    ```

    **Кастомные Type Guards с `is`:**

    Когда встроенных проверок недостаточно, можно создать свою функцию-guard:
    ```typescript
    interface Fish { swim(): void }
    interface Bird { fly(): void }

    // Функция возвращает "type predicate"
    function isFish(pet: Fish | Bird): pet is Fish {
      return (pet as Fish).swim !== undefined;
    }

    function move(pet: Fish | Bird) {
      if (isFish(pet)) {
        pet.swim();  // TypeScript знает — это Fish
      } else {
        pet.fly();   // TypeScript знает — это Bird
      }
    }
    ```

    **Проверка на `null`/`undefined`:**
    ```typescript
    function processValue(value: string | null | undefined) {
      if (value != null) {  // или value !== null && value !== undefined
        // Здесь value гарантированно string
        console.log(value.length);
      }
    }
    ```

    **`in` оператор — проверка наличия свойства:**
    ```typescript
    interface Admin { role: string; }
    interface User { email: string; }

    function isAdmin(person: Admin | User): person is Admin {
      return "role" in person;
    }
    ```
8.  **Union & Intersection Types**: Как объединять типы (`|`) и пересекать их (`&`)?

    **Ответ:**

    **Union Types (`|`)** — значение может быть **одним из** перечисленных типов:
    ```typescript
    type ID = string | number;

    let userId: ID;
    userId = "abc123";  // OK
    userId = 42;        // OK
    userId = true;      // Ошибка

    // Часто используется для состояний:
    type Status = "loading" | "success" | "error";

    // Или для разных вариантов ответа:
    type Response = SuccessResponse | ErrorResponse;
    ```

    **Работа с Union:**
    ```typescript
    function printId(id: string | number) {
      // Можно использовать только общие методы
      console.log(id.toString());  // OK — есть у обоих типов

      // Для специфичных методов нужен type guard
      if (typeof id === "string") {
        console.log(id.toUpperCase());
      }
    }
    ```

    **Intersection Types (`&`)** — значение должно соответствовать **всем** типам одновременно:
    ```typescript
    interface Person {
      name: string;
      age: number;
    }

    interface Employee {
      company: string;
      position: string;
    }

    // Комбинируем оба интерфейса
    type EmployeePerson = Person & Employee;

    const employee: EmployeePerson = {
      name: "John",
      age: 30,
      company: "Acme",
      position: "Developer"
    };
    // Все 4 поля обязательны!
    ```

    **Практические примеры:**
    ```typescript
    // Расширение пропсов компонента
    type ButtonProps = React.ButtonHTMLAttributes<HTMLButtonElement> & {
      variant: "primary" | "secondary";
      isLoading?: boolean;
    };

    // Добавление ID к любому типу
    type WithId<T> = T & { id: string };
    type UserWithId = WithId<User>;
    ```

    **Важное отличие:**
    - `Union (A | B)` — нужно соответствовать хотя бы одному типу
    - `Intersection (A & B)` — нужно соответствовать обоим типам

    **Discriminated Unions (размеченные объединения):**
    ```typescript
    // Паттерн для обработки разных состояний
    type RequestState =
      | { status: "idle" }
      | { status: "loading" }
      | { status: "success"; data: User[] }
      | { status: "error"; error: string };

    function handleState(state: RequestState) {
      switch (state.status) {
        case "loading":
          return <Spinner />;
        case "success":
          return <UserList users={state.data} />;  // data доступна!
        case "error":
          return <Error message={state.error} />;  // error доступен!
      }
    }
    ```

## 2. State Management & Data Fetching

### 🟢 Базовые

9.  **Server State vs Client State**: Почему данные с сервера лучше хранить отдельно от UI-стейта (открытые модалки, инпуты)?

    **Ответ:**

    **Client State (клиентское состояние)** — данные, которые живут только в браузере:
    - Открыта ли модалка
    - Текущее значение инпута
    - Выбранная вкладка
    - Тема оформления (темная/светлая)
    - Состояние UI-компонентов

    **Server State (серверное состояние)** — данные, которые приходят с бэкенда:
    - Список пользователей
    - Товары в каталоге
    - Данные профиля
    - Любые данные из API

    **Почему их нужно хранить отдельно:**

    1. **Разный жизненный цикл:**
       - Client state мгновенно обновляется
       - Server state требует запросов, может устареть, нужна синхронизация

    2. **Разные задачи:**
       - Client state: простое хранение и обновление
       - Server state: кэширование, дедупликация запросов, фоновое обновление, обработка ошибок

    3. **Сложность управления:**
       ```typescript
       // Плохо: всё в одном месте
       const [state, setState] = useState({
         users: [],           // серверные данные
         isLoading: false,    // состояние загрузки
         error: null,         // ошибки
         isModalOpen: false,  // UI состояние
         searchQuery: ""      // UI состояние
       });
       ```

       ```typescript
       // Хорошо: разделение ответственности
       // Server state — через React Query
       const { data: users, isLoading, error } = useQuery(['users'], fetchUsers);

       // Client state — локальный useState
       const [isModalOpen, setModalOpen] = useState(false);
       const [searchQuery, setSearchQuery] = useState("");
       ```

    4. **Преимущества разделения:**
       - Автоматическое кэширование серверных данных
       - Повторное использование данных между компонентами
       - Background refetching (фоновое обновление)
       - Оптимистичные обновления
       - Меньше бойлерплейта

    **Инструменты:**
    - Server state: React Query (TanStack Query), SWR, RTK Query
    - Client state: useState, useReducer, Zustand, Jotai
10. **React Query / SWR**: Основные концепции. Зачем нужны (кэширование, дедупликация запросов, автоматическое обновление)?

    **Ответ:**

    **React Query** (TanStack Query) и **SWR** — библиотеки для работы с серверным состоянием. Они решают типичные проблемы при работе с API.

    **Основные концепции:**

    **1. Кэширование:**
    ```typescript
    // Данные сохраняются в кэш по ключу
    const { data } = useQuery({
      queryKey: ['users'],
      queryFn: fetchUsers
    });

    // При повторном вызове — данные берутся из кэша мгновенно
    // Не нужно ждать ответа сервера
    ```

    **2. Дедупликация запросов:**
    ```typescript
    // Если 10 компонентов запрашивают ['users'],
    // реально выполнится только 1 запрос к серверу
    function UserCount() {
      const { data } = useQuery({ queryKey: ['users'], queryFn: fetchUsers });
      return <span>{data?.length}</span>;
    }

    function UserList() {
      const { data } = useQuery({ queryKey: ['users'], queryFn: fetchUsers });
      return <ul>{data?.map(u => <li>{u.name}</li>)}</ul>;
    }
    // Оба компонента получат одни и те же данные из одного запроса
    ```

    **3. Автоматическое обновление (Background Refetch):**
    ```typescript
    const { data } = useQuery({
      queryKey: ['users'],
      queryFn: fetchUsers,
      staleTime: 5 * 60 * 1000,      // данные "свежие" 5 минут
      refetchOnWindowFocus: true,    // обновить при возврате на вкладку
      refetchInterval: 30000,        // обновлять каждые 30 секунд
    });
    ```

    **4. Состояния загрузки и ошибок "из коробки":**
    ```typescript
    const { data, isLoading, isError, error, refetch } = useQuery({
      queryKey: ['user', id],
      queryFn: () => fetchUser(id)
    });

    if (isLoading) return <Spinner />;
    if (isError) return <Error message={error.message} />;
    return <User data={data} />;
    ```

    **5. Мутации (изменение данных):**
    ```typescript
    const mutation = useMutation({
      mutationFn: createUser,
      onSuccess: () => {
        // Инвалидируем кэш — данные обновятся
        queryClient.invalidateQueries({ queryKey: ['users'] });
      }
    });

    mutation.mutate({ name: "John", email: "john@example.com" });
    ```

    **Почему это лучше, чем useEffect + useState:**
    - Не нужно писать логику загрузки вручную
    - Автоматическая обработка race conditions
    - Retry при ошибках
    - Девтулы для отладки
    - Оптимистичные обновления
    - Pagination и infinite scroll из коробки

    **React Query vs SWR:**
    - React Query: больше возможностей, лучше для сложных случаев
    - SWR: проще API, легче для простых проектов
11. **Zustand / Redux Toolkit**: Чем отличаются от старого Redux? (Меньше бойлерплейта, хуки).

    **Ответ:**

    **Проблемы классического Redux:**
    - Много бойлерплейта: отдельные файлы для actions, action types, reducers
    - Сложная настройка (middleware, devtools)
    - Иммутабельные обновления вручную через spread-операторы
    - Подключение к компонентам через `connect()` HOC

    **Redux Toolkit (RTK)** — официальный способ писать Redux сегодня:
    ```typescript
    // Было (классический Redux):
    // actions.ts
    const ADD_TODO = 'ADD_TODO';
    const addTodo = (text) => ({ type: ADD_TODO, payload: text });

    // reducer.ts
    const todosReducer = (state = [], action) => {
      switch (action.type) {
        case ADD_TODO:
          return [...state, { text: action.payload, done: false }];
        default:
          return state;
      }
    };

    // Стало (RTK):
    import { createSlice } from '@reduxjs/toolkit';

    const todosSlice = createSlice({
      name: 'todos',
      initialState: [],
      reducers: {
        addTodo: (state, action) => {
          // Можно "мутировать" напрямую благодаря Immer
          state.push({ text: action.payload, done: false });
        }
      }
    });

    export const { addTodo } = todosSlice.actions;
    export default todosSlice.reducer;
    ```

    **Преимущества RTK:**
    - `createSlice` — actions и reducer в одном месте
    - Immer под капотом — можно писать "мутирующий" код
    - `configureStore` — автоматическая настройка devtools и middleware
    - RTK Query — встроенное решение для работы с API

    **Zustand** — минималистичная альтернатива:
    ```typescript
    import { create } from 'zustand';

    interface TodoStore {
      todos: Todo[];
      addTodo: (text: string) => void;
    }

    const useTodoStore = create<TodoStore>((set) => ({
      todos: [],
      addTodo: (text) => set((state) => ({
        todos: [...state.todos, { text, done: false }]
      }))
    }));

    // Использование в компоненте — просто хук:
    function TodoList() {
      const todos = useTodoStore((state) => state.todos);
      const addTodo = useTodoStore((state) => state.addTodo);
      // ...
    }
    ```

    **Преимущества Zustand:**
    - Минимум бойлерплейта — весь стор в одном файле
    - Не нужен Provider (работает вне React-дерева)
    - Размер ~1KB (vs ~10KB у RTK)
    - Простой API — только хуки
    - Встроенная поддержка middleware (persist, devtools)

    **Когда что выбрать:**
    - **RTK** — большие проекты, команда знакома с Redux, нужен RTK Query
    - **Zustand** — средние/малые проекты, хочется простоты и минимализма

### 🟡 Продвинутые

12. **Optimistic Updates**: Что такое оптимистичный UI? Как реализовать откат изменений при ошибке сервера?

    **Ответ:**

    **Оптимистичный UI** — это подход, при котором интерфейс обновляется **мгновенно**, не дожидаясь ответа сервера. Мы "оптимистично" предполагаем, что запрос выполнится успешно.

    **Почему это важно:**
    - Пользователь не ждёт — интерфейс отзывчивый
    - Создаётся ощущение быстрого приложения
    - Особенно заметно на медленных соединениях

    **Пример без оптимистичного UI:**
    ```typescript
    // Пользователь нажал "Лайк"
    // 1. Показываем спиннер
    // 2. Ждём ответ сервера (200-500ms)
    // 3. Обновляем UI
    // Пользователь ждёт — плохой UX
    ```

    **Пример с оптимистичным UI:**
    ```typescript
    // Пользователь нажал "Лайк"
    // 1. Сразу показываем лайк (+1)
    // 2. Отправляем запрос на сервер
    // 3. Если ошибка — откатываем изменения
    // Пользователь не ждёт — отличный UX
    ```

    **Реализация с React Query:**
    ```typescript
    const likeMutation = useMutation({
      mutationFn: (postId: string) => api.likePost(postId),

      // Перед запросом — оптимистично обновляем кэш
      onMutate: async (postId) => {
        // Отменяем исходящие запросы (чтобы не перезаписали наше обновление)
        await queryClient.cancelQueries({ queryKey: ['post', postId] });

        // Сохраняем предыдущее значение для отката
        const previousPost = queryClient.getQueryData(['post', postId]);

        // Оптимистично обновляем
        queryClient.setQueryData(['post', postId], (old: Post) => ({
          ...old,
          likes: old.likes + 1,
          isLiked: true
        }));

        // Возвращаем контекст с предыдущими данными
        return { previousPost };
      },

      // При ошибке — откатываем
      onError: (err, postId, context) => {
        queryClient.setQueryData(['post', postId], context?.previousPost);
        toast.error('Не удалось поставить лайк');
      },

      // После завершения (успех или ошибка) — синхронизируемся с сервером
      onSettled: (data, error, postId) => {
        queryClient.invalidateQueries({ queryKey: ['post', postId] });
      }
    });
    ```

    **Когда использовать:**
    - Лайки, закладки, подписки
    - Добавление/удаление из корзины
    - Отправка сообщений в чат
    - Любые действия, которые обычно успешны

    **Когда НЕ использовать:**
    - Платёжные операции
    - Действия с серьёзными последствиями
    - Когда высока вероятность ошибки

13. **Selectors**: Как избежать лишних ререндеров при выборе данных из глобального стора?

    **Ответ:**

    **Проблема:** Когда компонент подписывается на весь стор, он ререндерится при любом изменении, даже если нужные ему данные не изменились.

    ```typescript
    // Плохо: компонент ререндерится при ЛЮБОМ изменении стора
    const store = useStore();
    const userName = store.user.name;
    ```

    **Selector** — функция, которая извлекает только нужную часть состояния:
    ```typescript
    // Хорошо: компонент ререндерится только при изменении user.name
    const userName = useStore((state) => state.user.name);
    ```

    **Как это работает:**

    Библиотеки (Zustand, Redux) сравнивают результат селектора до и после изменения стора. Если результат тот же — ререндера не будет.

    **Проблема с созданием новых объектов:**
    ```typescript
    // Плохо: каждый раз создаётся новый объект — ререндер всегда
    const userData = useStore((state) => ({
      name: state.user.name,
      email: state.user.email
    }));
    // { name, email } !== { name, email } — это разные объекты!
    ```

    **Решения:**

    **1. Несколько отдельных селекторов:**
    ```typescript
    const name = useStore((state) => state.user.name);
    const email = useStore((state) => state.user.email);
    ```

    **2. Shallow comparison (поверхностное сравнение):**
    ```typescript
    import { useShallow } from 'zustand/react/shallow';

    const { name, email } = useStore(
      useShallow((state) => ({
        name: state.user.name,
        email: state.user.email
      }))
    );
    ```

    **3. Мемоизированные селекторы (Reselect для Redux):**
    ```typescript
    import { createSelector } from '@reduxjs/toolkit';

    // Мемоизированный селектор — пересчитывается только при изменении зависимостей
    const selectActiveUsers = createSelector(
      [(state) => state.users, (state) => state.filter],
      (users, filter) => users.filter(u => u.status === filter)
    );

    // Использование
    const activeUsers = useSelector(selectActiveUsers);
    ```

    **Практические правила:**
    - Выбирайте минимально необходимые данные
    - Избегайте создания новых объектов/массивов в селекторах
    - Для вычисляемых данных используйте мемоизацию
    - Проверяйте ререндеры через React DevTools Profiler

14. **Stale-while-revalidate**: Объясните стратегию кэширования "сначала покажи старое, потом обнови".

    **Ответ:**

    **Stale-While-Revalidate (SWR)** — стратегия кэширования, при которой:
    1. Сначала показываются данные из кэша (даже если устаревшие — "stale")
    2. Параллельно отправляется запрос за свежими данными ("revalidate")
    3. Когда приходит ответ — UI обновляется

    **Визуализация:**
    ```
    Пользователь открывает страницу
           |
           v
    [Мгновенно показываем кэш] -----> Пользователь уже видит контент!
           |
           v
    [Фоновый запрос к серверу]
           |
           v
    [Получили ответ — обновляем UI] --> Пользователь видит свежие данные
    ```

    **Преимущества:**
    - **Мгновенный отклик** — пользователь не ждёт
    - **Всегда свежие данные** — кэш обновляется в фоне
    - **Работа офлайн** — если нет сети, покажем кэш
    - **Снижение нагрузки** — не нужно ждать каждый запрос

    **Пример с React Query:**
    ```typescript
    const { data, isLoading, isFetching } = useQuery({
      queryKey: ['users'],
      queryFn: fetchUsers,
      staleTime: 5 * 60 * 1000,  // Данные "свежие" 5 минут
    });

    // isLoading — true только при первой загрузке (нет кэша)
    // isFetching — true при любом запросе (включая фоновое обновление)

    return (
      <div>
        {isLoading && <Spinner />}           {/* Только первый раз */}
        {isFetching && <RefreshIndicator />} {/* Фоновое обновление */}
        <UserList users={data} />
      </div>
    );
    ```

    **Когда происходит revalidation (обновление):**
    ```typescript
    const { data } = useQuery({
      queryKey: ['users'],
      queryFn: fetchUsers,
      staleTime: 0,                    // Данные сразу считаются устаревшими
      refetchOnWindowFocus: true,      // При возврате на вкладку
      refetchOnReconnect: true,        // При восстановлении сети
      refetchInterval: 30000,          // Каждые 30 секунд
    });
    ```

    **HTTP заголовок Cache-Control:**
    ```
    Cache-Control: max-age=0, stale-while-revalidate=86400
    ```
    Это говорит браузеру: "Данные устарели сразу, но можно показывать кэш ещё сутки, пока идёт перезапрос".

    **Когда использовать:**
    - Списки данных (пользователи, товары, статьи)
    - Данные, которые не критично показать устаревшими на секунду
    - Любой контент, где важна скорость отображения

    **Когда НЕ использовать:**
    - Данные в реальном времени (биржевые котировки)
    - Критичные данные (баланс счёта при оплате)
    - Одноразовые данные (OTP коды)

## 3. Frontend System Design (Архитектура)

### 🟢 Базовые

15. **Структура проекта**: Feature-sliced (кратко) vs группировка по типам файлов (components, hooks, utils). Плюсы и минусы.

    **Ответ:**

    **Группировка по типам файлов** (классический подход):
    ```
    src/
    ├── components/
    │   ├── Button.tsx
    │   ├── Header.tsx
    │   └── UserCard.tsx
    ├── hooks/
    │   ├── useAuth.ts
    │   └── useUsers.ts
    ├── utils/
    │   └── formatDate.ts
    ├── services/
    │   └── api.ts
    └── pages/
        ├── Home.tsx
        └── Profile.tsx
    ```

    **Плюсы:**
    - Простая и понятная структура
    - Легко найти файл по его типу
    - Хорошо для небольших проектов

    **Минусы:**
    - При росте проекта папки разбухают (100+ компонентов)
    - Связанные файлы разбросаны по разным папкам
    - Сложно понять, какие файлы относятся к одной фиче

    **Feature-Sliced Design (FSD)** — методология организации по фичам:
    ```
    src/
    ├── app/              # Инициализация, провайдеры, роутинг
    ├── pages/            # Страницы приложения
    ├── widgets/          # Крупные составные блоки (Header, Sidebar)
    ├── features/         # Бизнес-логика (авторизация, добавление в корзину)
    │   ├── auth/
    │   │   ├── ui/
    │   │   ├── model/
    │   │   └── api/
    │   └── cart/
    │       ├── ui/
    │       ├── model/
    │       └── api/
    ├── entities/         # Бизнес-сущности (User, Product)
    │   └── user/
    │       ├── ui/
    │       ├── model/
    │       └── api/
    └── shared/           # Переиспользуемый код (ui-kit, utils, api)
    ```

    **Плюсы FSD:**
    - Всё связанное с фичей в одном месте
    - Чёткие правила зависимостей (нижние слои не знают о верхних)
    - Масштабируется для больших проектов
    - Легко удалить или перенести фичу

    **Минусы FSD:**
    - Высокий порог входа
    - Избыточен для маленьких проектов
    - Требует дисциплины команды

    **Рекомендация:**
    - Маленький проект (1-3 разработчика) → группировка по типам
    - Средний/большой проект → Feature-based или FSD

16. **Композиция компонентов**: Как избежать "Prop Drilling" с помощью композиции (`children` prop)?

    **Ответ:**

    **Prop Drilling** — передача пропсов через много уровней компонентов, которые сами их не используют:

    ```typescript
    // Плохо: theme прокидывается через 3 уровня
    function App() {
      const [theme, setTheme] = useState('dark');
      return <Layout theme={theme} setTheme={setTheme} />;
    }

    function Layout({ theme, setTheme }) {
      return <Header theme={theme} setTheme={setTheme} />;
    }

    function Header({ theme, setTheme }) {
      return <ThemeToggle theme={theme} setTheme={setTheme} />;
    }

    function ThemeToggle({ theme, setTheme }) {
      return <button onClick={() => setTheme(/*...*/)}>Theme: {theme}</button>;
    }
    ```

    **Решение 1: Композиция через children**

    Вместо передачи пропсов вниз, передаём готовый компонент:

    ```typescript
    // Хорошо: Layout и Header ничего не знают о theme
    function App() {
      const [theme, setTheme] = useState('dark');

      return (
        <Layout>
          <Header>
            <ThemeToggle theme={theme} setTheme={setTheme} />
          </Header>
        </Layout>
      );
    }

    function Layout({ children }) {
      return <div className="layout">{children}</div>;
    }

    function Header({ children }) {
      return <header>{children}</header>;
    }
    ```

    **Решение 2: Render Props / Слоты**

    ```typescript
    function Card({ header, content, footer }) {
      return (
        <div className="card">
          <div className="card-header">{header}</div>
          <div className="card-content">{content}</div>
          <div className="card-footer">{footer}</div>
        </div>
      );
    }

    // Использование
    <Card
      header={<h2>Title</h2>}
      content={<p>Some content</p>}
      footer={<Button>Submit</Button>}
    />
    ```

    **Решение 3: Compound Components**

    ```typescript
    // Компонент-контейнер с дочерними компонентами
    function Tabs({ children, defaultTab }) {
      const [activeTab, setActiveTab] = useState(defaultTab);

      return (
        <TabsContext.Provider value={{ activeTab, setActiveTab }}>
          {children}
        </TabsContext.Provider>
      );
    }

    Tabs.List = function TabsList({ children }) { /* ... */ };
    Tabs.Tab = function Tab({ children, value }) { /* ... */ };
    Tabs.Panel = function TabsPanel({ children, value }) { /* ... */ };

    // Использование
    <Tabs defaultTab="first">
      <Tabs.List>
        <Tabs.Tab value="first">Tab 1</Tabs.Tab>
        <Tabs.Tab value="second">Tab 2</Tabs.Tab>
      </Tabs.List>
      <Tabs.Panel value="first">Content 1</Tabs.Panel>
      <Tabs.Panel value="second">Content 2</Tabs.Panel>
    </Tabs>
    ```

    **Когда что использовать:**
    - **children** — простые случаи, один слот
    - **Именованные слоты (header, footer)** — несколько мест для контента
    - **Context** — когда данные нужны на многих уровнях
    - **Compound Components** — сложные UI-компоненты (Tabs, Accordion, Select)

17. **Auth**: Где хранить токен доступа (localStorage vs Cookies)? Плюсы и минусы (XSS vs CSRF).

    **Ответ:**

    **Варианты хранения токенов:**

    | Способ | XSS уязвимость | CSRF уязвимость |
    |--------|---------------|-----------------|
    | localStorage | Да | Нет |
    | Cookies (без httpOnly) | Да | Да |
    | Cookies (httpOnly) | Нет | Да |

    **localStorage:**
    ```typescript
    // Сохранение
    localStorage.setItem('token', 'eyJhbG...');

    // Использование
    fetch('/api/data', {
      headers: { Authorization: `Bearer ${localStorage.getItem('token')}` }
    });
    ```

    **Плюсы:**
    - Простая реализация
    - Не отправляется автоматически — защита от CSRF
    - Работает для любых API (включая сторонние домены)

    **Минусы:**
    - **Уязвим к XSS**: Если злоумышленник внедрит скрипт, он получит токен через `localStorage.getItem('token')`

    **Cookies (httpOnly):**
    ```typescript
    // Сервер устанавливает cookie
    Set-Cookie: token=eyJhbG...; HttpOnly; Secure; SameSite=Strict

    // Браузер автоматически отправляет cookie
    fetch('/api/data', { credentials: 'include' });
    ```

    **Плюсы:**
    - **Защита от XSS**: JavaScript не может прочитать httpOnly cookie
    - Автоматическая отправка — меньше кода

    **Минусы:**
    - **Уязвим к CSRF**: Браузер отправляет cookie автоматически, даже с вредоносного сайта
    - Требует настройки CORS и SameSite

    **Защита от CSRF:**
    ```typescript
    // 1. SameSite=Strict — cookie отправляется только с вашего сайта
    Set-Cookie: token=...; SameSite=Strict

    // 2. CSRF-токен — дополнительный токен в заголовке
    fetch('/api/data', {
      headers: { 'X-CSRF-Token': csrfToken },
      credentials: 'include'
    });
    ```

    **Рекомендуемый подход:**

    ```typescript
    // Access Token — короткоживущий (15 мин), в памяти приложения
    let accessToken = null;

    // Refresh Token — долгоживущий, в httpOnly cookie
    // Автоматически обновляет access token

    async function fetchWithAuth(url) {
      if (!accessToken) {
        accessToken = await refreshAccessToken(); // Вызывает /api/refresh
      }

      return fetch(url, {
        headers: { Authorization: `Bearer ${accessToken}` }
      });
    }
    ```

    **Итог:**
    - **localStorage** — проще, но требует тщательной защиты от XSS (санитизация, CSP)
    - **httpOnly Cookies** — безопаснее при правильной настройке (SameSite, CSRF-токены)
    - **В памяти + Refresh в cookie** — лучший баланс безопасности

### 🟡 Продвинутые

18. **API Layer**: Зачем создавать инстанс axios/fetch? (Интерцепторы для токенов, обработки ошибок).

    **Ответ:**

    **API Layer** — централизованный слой для работы с HTTP-запросами. Вместо разбросанных fetch-вызовов создаётся единая точка входа.

    **Без API Layer (плохо):**
    ```typescript
    // В каждом компоненте/сервисе повторяется:
    const response = await fetch('https://api.example.com/users', {
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${getToken()}`
      }
    });

    if (!response.ok) {
      // Обработка ошибки
    }

    const data = await response.json();
    ```

    **С API Layer (хорошо):**
    ```typescript
    // api/client.ts
    import axios from 'axios';

    const apiClient = axios.create({
      baseURL: 'https://api.example.com',
      timeout: 10000,
      headers: { 'Content-Type': 'application/json' }
    });

    // Интерцептор для токена
    apiClient.interceptors.request.use((config) => {
      const token = getToken();
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      return config;
    });

    // Интерцептор для обработки ошибок
    apiClient.interceptors.response.use(
      (response) => response,
      async (error) => {
        // Автоматический refresh token при 401
        if (error.response?.status === 401) {
          const newToken = await refreshToken();
          error.config.headers.Authorization = `Bearer ${newToken}`;
          return apiClient.request(error.config);
        }

        // Централизованная обработка ошибок
        if (error.response?.status === 500) {
          toast.error('Ошибка сервера');
        }

        return Promise.reject(error);
      }
    );

    export default apiClient;
    ```

    **Использование:**
    ```typescript
    // api/users.ts
    import apiClient from './client';

    export const usersApi = {
      getAll: () => apiClient.get('/users'),
      getById: (id: string) => apiClient.get(`/users/${id}`),
      create: (data: CreateUserDto) => apiClient.post('/users', data),
      update: (id: string, data: UpdateUserDto) => apiClient.put(`/users/${id}`, data),
      delete: (id: string) => apiClient.delete(`/users/${id}`)
    };
    ```

    **Преимущества:**
    - **DRY** — базовый URL, заголовки, таймауты в одном месте
    - **Автоматическая авторизация** — токен добавляется ко всем запросам
    - **Refresh Token** — автоматическое обновление при истечении
    - **Централизованная обработка ошибок** — логирование, уведомления
    - **Типизация** — один раз описали типы ответов
    - **Тестируемость** — легко мокать один модуль

    **То же самое на чистом fetch:**
    ```typescript
    async function apiFetch<T>(endpoint: string, options?: RequestInit): Promise<T> {
      const token = getToken();

      const response = await fetch(`${BASE_URL}${endpoint}`, {
        ...options,
        headers: {
          'Content-Type': 'application/json',
          ...(token && { Authorization: `Bearer ${token}` }),
          ...options?.headers
        }
      });

      if (!response.ok) {
        if (response.status === 401) {
          await refreshToken();
          return apiFetch(endpoint, options); // Повторяем запрос
        }
        throw new ApiError(response.status, await response.json());
      }

      return response.json();
    }
    ```

19. **Error Handling**: Как централизованно обрабатывать ошибки API? (Global Error Boundary, Toast notifications).

    **Ответ:**

    Централизованная обработка ошибок позволяет:
    - Показывать понятные сообщения пользователю
    - Логировать ошибки для отладки
    - Не дублировать try-catch в каждом компоненте

    **1. Error Boundary — для критических ошибок React:**

    ```typescript
    import { Component, ErrorInfo, ReactNode } from 'react';

    interface Props {
      children: ReactNode;
      fallback?: ReactNode;
    }

    interface State {
      hasError: boolean;
      error?: Error;
    }

    class ErrorBoundary extends Component<Props, State> {
      state: State = { hasError: false };

      static getDerivedStateFromError(error: Error): State {
        return { hasError: true, error };
      }

      componentDidCatch(error: Error, errorInfo: ErrorInfo) {
        // Отправляем в систему мониторинга (Sentry, LogRocket)
        logErrorToService(error, errorInfo);
      }

      render() {
        if (this.state.hasError) {
          return this.props.fallback || <ErrorPage error={this.state.error} />;
        }
        return this.props.children;
      }
    }

    // Использование
    <ErrorBoundary fallback={<ErrorPage />}>
      <App />
    </ErrorBoundary>
    ```

    **2. Централизованный обработчик API ошибок:**

    ```typescript
    // utils/errorHandler.ts
    export function handleApiError(error: unknown) {
      if (axios.isAxiosError(error)) {
        const status = error.response?.status;
        const message = error.response?.data?.message;

        switch (status) {
          case 400:
            toast.error(message || 'Некорректный запрос');
            break;
          case 401:
            toast.error('Сессия истекла. Войдите снова');
            // Редирект на логин
            break;
          case 403:
            toast.error('Доступ запрещён');
            break;
          case 404:
            toast.error('Ресурс не найден');
            break;
          case 422:
            // Ошибки валидации — показываем детально
            showValidationErrors(error.response?.data?.errors);
            break;
          case 500:
            toast.error('Ошибка сервера. Попробуйте позже');
            break;
          default:
            toast.error('Произошла ошибка');
        }

        // Логируем для отладки
        console.error('API Error:', { status, message, url: error.config?.url });
      } else {
        toast.error('Неизвестная ошибка');
        console.error('Unknown error:', error);
      }
    }
    ```

    **3. Интеграция с React Query:**

    ```typescript
    // Глобальная обработка в QueryClient
    const queryClient = new QueryClient({
      queryCache: new QueryCache({
        onError: (error) => handleApiError(error)
      }),
      mutationCache: new MutationCache({
        onError: (error) => handleApiError(error)
      })
    });

    // Или на уровне отдельного запроса
    const { data } = useQuery({
      queryKey: ['users'],
      queryFn: fetchUsers,
      onError: (error) => {
        // Кастомная обработка для этого запроса
      }
    });
    ```

    **4. Библиотеки для Toast уведомлений:**
    - `react-hot-toast` — минималистичная
    - `react-toastify` — больше возможностей
    - `sonner` — современная и красивая

    ```typescript
    import toast from 'react-hot-toast';

    // Простое использование
    toast.success('Сохранено!');
    toast.error('Ошибка при сохранении');
    toast.loading('Загрузка...');

    // С промисом
    toast.promise(saveData(), {
      loading: 'Сохранение...',
      success: 'Данные сохранены!',
      error: 'Не удалось сохранить'
    });
    ```

    **Итог — многоуровневая обработка:**
    1. **Error Boundary** — ловит падения React-компонентов
    2. **API Interceptors** — обрабатывает HTTP-ошибки глобально
    3. **React Query onError** — для специфичной логики запросов
    4. **Toast** — показывает пользователю понятные сообщения

20. **Reusable Components**: Принципы проектирования переиспользуемых UI-компонентов (Headless UI, разделение логики и представления).

    **Ответ:**

    **Переиспользуемый компонент** — компонент, который можно использовать в разных местах приложения без изменения его кода.

    **Принципы проектирования:**

    **1. Разделение логики и представления:**

    ```typescript
    // Плохо: логика и UI смешаны
    function Dropdown() {
      const [isOpen, setIsOpen] = useState(false);
      const [selected, setSelected] = useState(null);

      return (
        <div className="dropdown">
          <button onClick={() => setIsOpen(!isOpen)}>
            {selected || 'Выберите...'}
          </button>
          {isOpen && (
            <ul className="dropdown-menu">
              {/* жёстко заданные стили и разметка */}
            </ul>
          )}
        </div>
      );
    }

    // Хорошо: хук с логикой отдельно
    function useDropdown<T>(options: T[]) {
      const [isOpen, setIsOpen] = useState(false);
      const [selected, setSelected] = useState<T | null>(null);

      const toggle = () => setIsOpen(!isOpen);
      const select = (option: T) => {
        setSelected(option);
        setIsOpen(false);
      };

      return { isOpen, selected, toggle, select, options };
    }

    // UI можно кастомизировать
    function MyDropdown() {
      const dropdown = useDropdown(['Apple', 'Banana', 'Orange']);

      return (
        <div className="my-custom-dropdown">
          {/* Своя разметка, свои стили */}
        </div>
      );
    }
    ```

    **2. Headless UI — компоненты без стилей:**

    Библиотеки типа Headless UI, Radix UI, React Aria предоставляют только логику и доступность, а стили — ваши.

    ```typescript
    import { Menu } from '@headlessui/react';

    function MyMenu() {
      return (
        <Menu>
          <Menu.Button className="my-button">Options</Menu.Button>
          <Menu.Items className="my-menu">
            <Menu.Item>
              {({ active }) => (
                <a className={active ? 'bg-blue-500' : ''}>Edit</a>
              )}
            </Menu.Item>
          </Menu.Items>
        </Menu>
      );
    }
    ```

    **Преимущества Headless:**
    - Полный контроль над стилями
    - Доступность (a11y) из коробки
    - Соответствует вашему дизайну, а не дизайну библиотеки

    **3. Гибкий API через пропсы:**

    ```typescript
    interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'ghost';
      size?: 'sm' | 'md' | 'lg';
      isLoading?: boolean;
      leftIcon?: React.ReactNode;
      rightIcon?: React.ReactNode;
    }

    function Button({
      variant = 'primary',
      size = 'md',
      isLoading,
      leftIcon,
      rightIcon,
      children,
      className,
      disabled,
      ...props
    }: ButtonProps) {
      return (
        <button
          className={cn(
            'btn',
            `btn-${variant}`,
            `btn-${size}`,
            className
          )}
          disabled={disabled || isLoading}
          {...props}
        >
          {isLoading ? <Spinner /> : leftIcon}
          {children}
          {rightIcon}
        </button>
      );
    }
    ```

    **4. Compound Components для сложных UI:**

    ```typescript
    // Вместо одного компонента с кучей пропсов
    <Card
      title="Hello"
      subtitle="World"
      image="/img.png"
      footer={<Button>Click</Button>}
    />

    // Гибкая композиция
    <Card>
      <Card.Image src="/img.png" />
      <Card.Body>
        <Card.Title>Hello</Card.Title>
        <Card.Text>World</Card.Text>
      </Card.Body>
      <Card.Footer>
        <Button>Click</Button>
      </Card.Footer>
    </Card>
    ```

    **5. Принцип инверсии контроля (Render Props / Children):**

    ```typescript
    // Компонент контролирует ЧТО рендерить
    function List({ items, renderItem }) {
      return (
        <ul>
          {items.map((item, index) => (
            <li key={index}>{renderItem(item)}</li>
          ))}
        </ul>
      );
    }

    // Использование — вы решаете КАК рендерить
    <List
      items={users}
      renderItem={(user) => <UserCard user={user} />}
    />
    ```

    **Чек-лист хорошего компонента:**
    - Не зависит от внешнего состояния (принимает данные через пропсы)
    - Позволяет кастомизировать стили (className, style)
    - Прокидывает нативные атрибуты (...rest props)
    - Доступен (keyboard navigation, ARIA)
    - Типизирован (TypeScript)
    - Имеет разумные значения по умолчанию

## 4. Performance & Web Vitals

### 🟢 Базовые

21. **Debounce vs Throttle**: В чем разница? Примеры использования (поиск, скролл).

    **Ответ:**

    Обе техники ограничивают частоту вызова функции, но делают это по-разному.

    **Debounce (отложенный вызов):**
    Функция выполнится только после того, как пользователь **прекратит действие** на определённое время.

    ```typescript
    // Поиск: запрос отправится через 300мс после того,
    // как пользователь перестанет печатать
    function debounce(fn: Function, delay: number) {
      let timeoutId: NodeJS.Timeout;
      return (...args: any[]) => {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => fn(...args), delay);
      };
    }

    const debouncedSearch = debounce((query: string) => {
      fetch(`/api/search?q=${query}`);
    }, 300);

    // При вводе "react":
    // r -> ждём... a -> ждём... c -> ждём... t -> ждём 300мс -> запрос!
    ```

    **Throttle (ограничение частоты):**
    Функция выполняется **не чаще** чем раз в указанный интервал, даже если событие происходит постоянно.

    ```typescript
    // Скролл: обработчик вызывается максимум раз в 100мс
    function throttle(fn: Function, limit: number) {
      let inThrottle = false;
      return (...args: any[]) => {
        if (!inThrottle) {
          fn(...args);
          inThrottle = true;
          setTimeout(() => inThrottle = false, limit);
        }
      };
    }

    const throttledScroll = throttle(() => {
      console.log('Scroll position:', window.scrollY);
    }, 100);

    window.addEventListener('scroll', throttledScroll);
    ```

    **Когда что использовать:**

    | Сценарий | Техника | Почему |
    |----------|---------|--------|
    | Поиск по вводу | Debounce | Ждём, пока пользователь закончит печатать |
    | Resize окна | Debounce | Пересчёт layout нужен после завершения |
    | Автосохранение | Debounce | Сохраняем после паузы в редактировании |
    | Scroll (бесконечная загрузка) | Throttle | Нужно регулярно проверять позицию |
    | Движение мыши | Throttle | Нужны обновления, но не 60 раз/сек |
    | Клик по кнопке "Отправить" | Throttle | Защита от двойного клика |

    **В React:**
    ```typescript
    import { useDeferredValue, useMemo } from 'react';
    import { useDebouncedCallback } from 'use-debounce';

    function SearchComponent() {
      const [query, setQuery] = useState('');

      // Вариант 1: библиотека use-debounce
      const debouncedSearch = useDebouncedCallback((value: string) => {
        fetchResults(value);
      }, 300);

      // Вариант 2: встроенный useDeferredValue (React 18+)
      const deferredQuery = useDeferredValue(query);

      const results = useMemo(() =>
        searchItems(deferredQuery), [deferredQuery]
      );
    }
    ```

22. **Lazy Loading**: Как лениво загружать картинки (`loading="lazy"`) и компоненты (`React.lazy`)?

    **Ответ:**

    **Lazy Loading** — загрузка ресурсов только когда они нужны. Это ускоряет начальную загрузку страницы.

    **1. Ленивая загрузка изображений:**

    ```html
    <!-- Нативный атрибут loading="lazy" -->
    <img
      src="photo.jpg"
      loading="lazy"
      alt="Фото"
      width="400"
      height="300"
    />
    ```

    Браузер загрузит изображение только когда оно приблизится к viewport (область видимости). Важно указывать `width` и `height`, чтобы избежать сдвигов layout (CLS).

    **Поддержка:** Все современные браузеры. Для старых — полифил или Intersection Observer.

    ```typescript
    // Intersection Observer для кастомной логики
    function LazyImage({ src, alt }: { src: string; alt: string }) {
      const imgRef = useRef<HTMLImageElement>(null);
      const [isLoaded, setIsLoaded] = useState(false);

      useEffect(() => {
        const observer = new IntersectionObserver(([entry]) => {
          if (entry.isIntersecting) {
            setIsLoaded(true);
            observer.disconnect();
          }
        });

        if (imgRef.current) observer.observe(imgRef.current);
        return () => observer.disconnect();
      }, []);

      return (
        <img
          ref={imgRef}
          src={isLoaded ? src : 'placeholder.jpg'}
          alt={alt}
        />
      );
    }
    ```

    **2. Ленивая загрузка React-компонентов:**

    ```typescript
    import { lazy, Suspense } from 'react';

    // Компонент загрузится только при рендере
    const HeavyChart = lazy(() => import('./HeavyChart'));
    const AdminPanel = lazy(() => import('./AdminPanel'));

    function App() {
      return (
        <div>
          <Header />

          {/* Suspense показывает fallback пока компонент загружается */}
          <Suspense fallback={<Spinner />}>
            <HeavyChart />
          </Suspense>

          {/* Загрузится только если пользователь — админ */}
          {isAdmin && (
            <Suspense fallback={<div>Загрузка панели...</div>}>
              <AdminPanel />
            </Suspense>
          )}
        </div>
      );
    }
    ```

    **3. Code Splitting по роутам (React Router):**

    ```typescript
    import { lazy, Suspense } from 'react';
    import { Routes, Route } from 'react-router-dom';

    // Каждая страница — отдельный chunk
    const Home = lazy(() => import('./pages/Home'));
    const Profile = lazy(() => import('./pages/Profile'));
    const Settings = lazy(() => import('./pages/Settings'));

    function App() {
      return (
        <Suspense fallback={<PageLoader />}>
          <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/profile" element={<Profile />} />
            <Route path="/settings" element={<Settings />} />
          </Routes>
        </Suspense>
      );
    }
    ```

    **4. Предзагрузка (Preload) для улучшения UX:**

    ```typescript
    // Загрузить компонент заранее (например, при hover)
    const Settings = lazy(() => import('./pages/Settings'));

    function NavLink() {
      const preloadSettings = () => {
        import('./pages/Settings'); // Начнёт загрузку
      };

      return (
        <Link
          to="/settings"
          onMouseEnter={preloadSettings}
        >
          Настройки
        </Link>
      );
    }
    ```

    **Что даёт Lazy Loading:**
    - Меньше начальный bundle → быстрее First Paint
    - Экономия трафика — не грузим то, что не нужно
    - Лучшие метрики Core Web Vitals

23. **Reflow vs Repaint**: Что это и какие CSS свойства вызывают дорогие перерисовки?

    **Ответ:**

    Браузер отображает страницу в несколько этапов:
    1. **Parse** — парсинг HTML/CSS
    2. **Layout (Reflow)** — расчёт размеров и позиций элементов
    3. **Paint (Repaint)** — отрисовка пикселей (цвета, границы, тени)
    4. **Composite** — склейка слоёв

    **Reflow (Layout):**
    Пересчёт геометрии элементов. **Дорогая операция**, так как изменение одного элемента может повлиять на все остальные.

    **Что вызывает Reflow:**
    ```css
    /* Геометрические свойства */
    width, height, padding, margin, border-width
    top, left, right, bottom
    font-size, line-height
    display, position, float
    ```

    ```javascript
    // JavaScript операции, вызывающие Reflow:
    element.offsetHeight;     // Чтение размеров
    element.getBoundingClientRect();
    window.getComputedStyle(element);
    element.scrollTop;        // Чтение scroll позиции
    ```

    **Repaint:**
    Перерисовка пикселей без изменения геометрии. **Дешевле** чем reflow.

    **Что вызывает только Repaint:**
    ```css
    color, background-color
    visibility (но не display!)
    border-color, outline
    box-shadow
    ```

    **Composite-only свойства (самые дешёвые):**
    ```css
    /* Эти свойства НЕ вызывают reflow/repaint */
    transform: translateX(100px);  /* вместо left: 100px */
    transform: scale(1.2);         /* вместо width/height */
    opacity: 0.5;                  /* вместо visibility */
    ```

    **Оптимизация:**

    ```typescript
    // Плохо: вызывает reflow на каждой итерации
    for (let i = 0; i < 100; i++) {
      element.style.left = element.offsetLeft + 10 + 'px';
      //                   ^ чтение вызывает forced reflow
    }

    // Хорошо: читаем один раз, пишем один раз
    const left = element.offsetLeft;
    element.style.left = left + 1000 + 'px';

    // Ещё лучше: используем transform
    element.style.transform = `translateX(${left + 1000}px)`;
    ```

    **Анимации — правильный подход:**

    ```css
    /* Плохо: анимация width вызывает постоянный reflow */
    .box {
      animation: grow 1s;
    }
    @keyframes grow {
      from { width: 100px; }
      to { width: 200px; }
    }

    /* Хорошо: transform работает на GPU */
    .box {
      animation: grow 1s;
      will-change: transform; /* Подсказка браузеру */
    }
    @keyframes grow {
      from { transform: scaleX(1); }
      to { transform: scaleX(2); }
    }
    ```

    **Практические правила:**
    - Анимируйте только `transform` и `opacity`
    - Избегайте чтения layout-свойств в циклах
    - Группируйте DOM-изменения (DocumentFragment, requestAnimationFrame)
    - Используйте `will-change` для сложных анимаций (но не злоупотребляйте)

### 🟡 Продвинутые

24. **Core Web Vitals**: Что такое LCP, CLS, INP? Как их оптимизировать?

    **Ответ:**

    **Core Web Vitals** — метрики Google для оценки пользовательского опыта. Влияют на SEO-ранжирование.

    **LCP (Largest Contentful Paint) — Загрузка**

    Время до отрисовки самого большого видимого элемента (обычно изображение или блок текста).

    | Оценка | Время |
    |--------|-------|
    | Хорошо | ≤ 2.5с |
    | Нужна работа | 2.5-4с |
    | Плохо | > 4с |

    **Как оптимизировать LCP:**
    ```html
    <!-- 1. Preload критичных ресурсов -->
    <link rel="preload" href="hero-image.jpg" as="image">
    <link rel="preload" href="main-font.woff2" as="font" crossorigin>

    <!-- 2. Оптимизация изображений -->
    <img
      src="hero.jpg"
      srcset="hero-400.jpg 400w, hero-800.jpg 800w"
      sizes="(max-width: 600px) 400px, 800px"
      fetchpriority="high"
    />

    <!-- 3. Критический CSS inline -->
    <style>
      /* Только стили для above-the-fold контента */
    </style>
    ```

    ```typescript
    // 4. SSR/SSG вместо client-side rendering
    // 5. Оптимизация сервера (CDN, сжатие, кэширование)
    ```

    **CLS (Cumulative Layout Shift) — Визуальная стабильность**

    Сумма всех неожиданных сдвигов layout. Происходит когда элементы "прыгают".

    | Оценка | Значение |
    |--------|----------|
    | Хорошо | ≤ 0.1 |
    | Нужна работа | 0.1-0.25 |
    | Плохо | > 0.25 |

    **Причины CLS и решения:**
    ```html
    <!-- Проблема: изображение без размеров -->
    <img src="photo.jpg" />  <!-- Когда загрузится — всё сдвинется -->

    <!-- Решение: указываем размеры -->
    <img src="photo.jpg" width="800" height="600" />
    <!-- Или через CSS aspect-ratio -->
    <style>
      img { aspect-ratio: 4/3; width: 100%; }
    </style>
    ```

    ```typescript
    // Проблема: динамически вставленный контент
    // Решение: резервируем место
    function AdBanner() {
      return (
        <div style={{ minHeight: '250px' }}>  {/* Резервируем место */}
          {adLoaded ? <Ad /> : <Skeleton />}
        </div>
      );
    }

    // Проблема: шрифты вызывают сдвиг (FOUT)
    // Решение: font-display и size-adjust
    ```

    ```css
    @font-face {
      font-family: 'MyFont';
      src: url('font.woff2') format('woff2');
      font-display: swap;
      size-adjust: 100.5%; /* Подгоняем под fallback */
    }
    ```

    **INP (Interaction to Next Paint) — Отзывчивость**

    Время от взаимодействия пользователя (клик, tap, нажатие клавиши) до визуального отклика. Заменил FID в 2024.

    | Оценка | Время |
    |--------|-------|
    | Хорошо | ≤ 200мс |
    | Нужна работа | 200-500мс |
    | Плохо | > 500мс |

    **Как оптимизировать INP:**
    ```typescript
    // 1. Разбивайте долгие задачи
    // Плохо: блокирует main thread
    function processAllItems(items: Item[]) {
      items.forEach(item => heavyProcessing(item));
    }

    // Хорошо: даём браузеру "дышать"
    async function processAllItems(items: Item[]) {
      for (const item of items) {
        heavyProcessing(item);
        // Даём браузеру обработать события
        await new Promise(r => setTimeout(r, 0));
        // Или используем scheduler.yield() в новых браузерах
      }
    }

    // 2. Используйте transitions вместо мгновенных изменений
    // 3. Web Workers для тяжёлых вычислений
    const worker = new Worker('heavy-task.js');
    worker.postMessage(data);
    worker.onmessage = (e) => updateUI(e.data);
    ```

    **Измерение Web Vitals:**
    ```typescript
    import { onLCP, onCLS, onINP } from 'web-vitals';

    onLCP(console.log);
    onCLS(console.log);
    onINP(console.log);
    ```

25. **Virtualization**: Как отобразить список из 10 000 элементов без лагов? (Концепция виртуального скролла).

    **Ответ:**

    **Проблема:** Рендеринг 10 000 DOM-элементов — это 10 000 узлов в памяти, 10 000 элементов для layout. Браузер "умрёт".

    **Виртуализация:** Рендерим только видимые элементы (+ небольшой буфер). При скролле — пересоздаём элементы с новыми данными.

    ```
    ┌─────────────────────┐
    │    Элемент 1        │  ← Не рендерится (выше viewport)
    │    Элемент 2        │
    ├─────────────────────┤ ◄─ Верх viewport
    │    Элемент 3        │  ← Рендерится
    │    Элемент 4        │  ← Рендерится
    │    Элемент 5        │  ← Рендерится (видимая область)
    │    Элемент 6        │  ← Рендерится
    │    Элемент 7        │  ← Рендерится
    ├─────────────────────┤ ◄─ Низ viewport
    │    Элемент 8        │
    │    ...              │  ← Не рендерится (ниже viewport)
    │    Элемент 10000    │
    └─────────────────────┘
    ```

    **Библиотеки:**
    - `@tanstack/react-virtual` (рекомендуется) — современная, гибкая
    - `react-window` — компактная, от автора react-virtualized
    - `react-virtualized` — больше возможностей, но тяжелее

    **Пример с @tanstack/react-virtual:**
    ```typescript
    import { useVirtualizer } from '@tanstack/react-virtual';

    function VirtualList({ items }: { items: Item[] }) {
      const parentRef = useRef<HTMLDivElement>(null);

      const virtualizer = useVirtualizer({
        count: items.length,        // 10 000 элементов
        getScrollElement: () => parentRef.current,
        estimateSize: () => 50,     // Примерная высота элемента
        overscan: 5,                // Буфер: рендерим +5 элементов сверху/снизу
      });

      return (
        <div
          ref={parentRef}
          style={{ height: '400px', overflow: 'auto' }}
        >
          {/* Контейнер с полной высотой для правильного скроллбара */}
          <div style={{ height: virtualizer.getTotalSize(), position: 'relative' }}>
            {virtualizer.getVirtualItems().map((virtualItem) => (
              <div
                key={virtualItem.key}
                style={{
                  position: 'absolute',
                  top: 0,
                  left: 0,
                  width: '100%',
                  height: `${virtualItem.size}px`,
                  transform: `translateY(${virtualItem.start}px)`,
                }}
              >
                {items[virtualItem.index].name}
              </div>
            ))}
          </div>
        </div>
      );
    }
    ```

    **Элементы переменной высоты:**
    ```typescript
    const virtualizer = useVirtualizer({
      count: items.length,
      getScrollElement: () => parentRef.current,
      estimateSize: () => 50,
      // Измеряем реальный размер после рендера
      measureElement: (el) => el.getBoundingClientRect().height,
    });
    ```

    **Виртуализация таблиц и гридов:**
    ```typescript
    // Для таблиц с фиксированными колонками
    import { useVirtualizer } from '@tanstack/react-virtual';

    // Для 2D гридов (виртуализация по X и Y)
    const rowVirtualizer = useVirtualizer({ /* ... */ });
    const columnVirtualizer = useVirtualizer({ /* ... */ });
    ```

    **Когда НЕ нужна виртуализация:**
    - Меньше 100-200 элементов — обычный рендер справится
    - Элементы очень простые (текст) — DOM справится
    - Нужен поиск по странице (Ctrl+F не найдёт не отрендеренное)

26. **Bundle Size**: Как анализировать размер бандла? Что такое Tree Shaking и как он работает?

    **Ответ:**

    **Почему размер важен:**
    - Большой bundle = долгая загрузка = плохой UX
    - Каждый KB JavaScript нужно скачать, распарсить, выполнить
    - На слабых устройствах парсинг JS может занять секунды

    **Анализ размера бандла:**

    ```bash
    # 1. Webpack Bundle Analyzer
    npm install --save-dev webpack-bundle-analyzer

    # В webpack.config.js:
    const BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;
    plugins: [new BundleAnalyzerPlugin()]

    # 2. Vite — встроенный анализ
    npx vite build --report

    # 3. source-map-explorer — анализ по source maps
    npx source-map-explorer dist/main.js

    # 4. bundlephobia.com — проверка размера npm-пакетов перед установкой
    ```

    **Tree Shaking — удаление неиспользуемого кода:**

    ```typescript
    // utils.ts
    export function usedFunction() { return 'used'; }
    export function unusedFunction() { return 'unused'; }

    // app.ts
    import { usedFunction } from './utils';
    console.log(usedFunction());

    // После Tree Shaking: unusedFunction удалена из бандла
    ```

    **Как работает Tree Shaking:**
    1. Использует ES Modules (`import`/`export`) — они статически анализируемы
    2. Bundler строит граф зависимостей
    3. Удаляет код, на который нет ссылок

    **Почему Tree Shaking может не работать:**

    ```typescript
    // 1. CommonJS не поддерживает tree shaking
    const _ = require('lodash');  // Плохо — весь lodash в бандле
    import { map } from 'lodash'; // Хорошо — но зависит от настроек lodash

    // Лучше: lodash-es или отдельные модули
    import map from 'lodash/map';

    // 2. Side effects мешают удалению
    // package.json библиотеки должен указать:
    {
      "sideEffects": false,  // или список файлов с side effects
    }

    // 3. Динамические импорты не анализируются
    import(someVariable);  // Bundler не знает что импортировать
    ```

    **Практические советы по уменьшению бандла:**

    ```typescript
    // 1. Импортируйте только нужное
    // Плохо:
    import * as _ from 'lodash';
    // Хорошо:
    import debounce from 'lodash/debounce';

    // 2. Используйте легковесные альтернативы
    // moment.js (300KB) → date-fns (tree-shakeable) или dayjs (2KB)
    // lodash → lodash-es или нативные методы

    // 3. Code splitting — разбивайте на chunks
    const HeavyComponent = lazy(() => import('./HeavyComponent'));

    // 4. Динамические импорты для редко используемого кода
    async function handleRareAction() {
      const { rareModule } = await import('./rareModule');
      rareModule.doSomething();
    }
    ```

    **Целевые размеры:**
    - Начальный JS: < 200KB (gzipped)
    - Для критического пути: < 50KB
    - Каждый route chunk: < 100KB

## 5. Build Tools & Ecosystem

### 🟢 Базовые

27. **NPM vs Yarn vs PNPM**: Базовые отличия (скорость, структура `node_modules`).

    **Ответ:**

    **NPM (Node Package Manager):**
    - Стандартный менеджер, идёт с Node.js
    - `node_modules` с плоской структурой (hoisting)
    - `package-lock.json` для фиксации версий

    ```bash
    npm install           # Установка зависимостей
    npm install lodash    # Добавить пакет
    npm run build         # Запуск скрипта
    ```

    **Yarn:**
    - Создан Facebook как альтернатива NPM
    - Быстрее за счёт параллельных загрузок и кэширования
    - `yarn.lock` — более детерминированный
    - Yarn 2+ (Berry): Plug'n'Play — без node_modules!

    ```bash
    yarn                  # Установка
    yarn add lodash       # Добавить пакет
    yarn build            # Запуск скрипта (без run)
    ```

    **PNPM:**
    - Самый эффективный по дисковому пространству
    - Все пакеты хранятся в глобальном store, в проекте — симлинки
    - Строгая структура node_modules — нет "phantom dependencies"

    ```bash
    pnpm install          # Установка
    pnpm add lodash       # Добавить пакет
    pnpm build            # Запуск скрипта
    ```

    **Сравнение структуры node_modules:**

    ```
    # NPM/Yarn Classic — плоская структура (hoisting)
    node_modules/
    ├── react/
    ├── lodash/           # Поднят из зависимости
    └── some-package/

    # Проблема: можно импортировать lodash, даже если он
    # не в ваших dependencies (phantom dependency)

    # PNPM — строгая структура
    node_modules/
    ├── .pnpm/            # Все пакеты здесь (симлинки в store)
    │   ├── react@18.2.0/
    │   └── lodash@4.17.21/
    ├── react -> .pnpm/react@18.2.0/
    └── # только ваши прямые зависимости!
    ```

    **Скорость (приблизительно):**

    | Операция | NPM | Yarn | PNPM |
    |----------|-----|------|------|
    | Чистая установка | Медленно | Быстро | Быстро |
    | С кэшем | Средне | Быстро | Очень быстро |
    | С lock-файлом | Быстро | Быстро | Очень быстро |

    **Экономия места (PNPM):**
    - 10 проектов с React = 1 копия React на диске
    - NPM/Yarn: 10 копий в каждом node_modules

    **Что выбрать:**
    - **NPM** — если нет особых требований, уже везде есть
    - **Yarn** — если команда привыкла, нужны workspaces
    - **PNPM** — монорепозитории, много проектов, экономия места

28. **Linters & Formatters**: Зачем нужны ESLint и Prettier? Как настроить их совместную работу?

    **Ответ:**

    **ESLint — линтер (поиск ошибок и проблем):**
    - Находит потенциальные баги (`no-unused-vars`, `no-undef`)
    - Проверяет качество кода (`no-console`, `complexity`)
    - Применяет best practices React (`exhaustive-deps`)

    **Prettier — форматтер (стиль кода):**
    - Отступы, кавычки, точки с запятой
    - Переносы строк, длина строки
    - Единообразие во всём проекте

    **Почему нужны оба:**
    - ESLint: "У тебя неиспользуемая переменная" — **ошибка**
    - Prettier: "Используй одинарные кавычки" — **стиль**

    **Настройка совместной работы:**

    ```bash
    npm install -D eslint prettier eslint-config-prettier
    ```

    ```javascript
    // .eslintrc.js
    module.exports = {
      extends: [
        'eslint:recommended',
        'plugin:react/recommended',
        'plugin:@typescript-eslint/recommended',
        'prettier'  // Должен быть ПОСЛЕДНИМ — отключает конфликтующие правила
      ],
      rules: {
        // Ваши кастомные правила ESLint
        'no-console': 'warn',
        'react/prop-types': 'off',
      }
    };
    ```

    ```json
    // .prettierrc
    {
      "semi": true,
      "singleQuote": true,
      "tabWidth": 2,
      "trailingComma": "es5",
      "printWidth": 100
    }
    ```

    **Интеграция в workflow:**

    ```json
    // package.json
    {
      "scripts": {
        "lint": "eslint src --ext .ts,.tsx",
        "lint:fix": "eslint src --ext .ts,.tsx --fix",
        "format": "prettier --write src/**/*.{ts,tsx,css}"
      }
    }
    ```

    **Автоформатирование при коммите (рекомендуется):**

    ```bash
    npm install -D husky lint-staged
    npx husky init
    ```

    ```json
    // package.json
    {
      "lint-staged": {
        "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
        "*.{css,json,md}": ["prettier --write"]
      }
    }
    ```

    **Настройка VS Code:**
    ```json
    // .vscode/settings.json
    {
      "editor.defaultFormatter": "esbenp.prettier-vscode",
      "editor.formatOnSave": true,
      "editor.codeActionsOnSave": {
        "source.fixAll.eslint": true
      }
    }
    ```

    **Итог:**
    - ESLint — **что** писать (логика, ошибки)
    - Prettier — **как** писать (форматирование)
    - `eslint-config-prettier` — отключает правила ESLint, которые конфликтуют с Prettier
    - Запускайте оба при коммите через lint-staged

### 🟡 Продвинутые

29. **Vite vs Webpack**: Почему Vite быстрее в dev-режиме? (ES Modules vs Bundling).

    **Ответ:**

    **Webpack — традиционный bundler:**
    ```
    1. Читает все файлы проекта
    2. Строит граф зависимостей
    3. Бандлит ВСЁ в один/несколько файлов
    4. Только потом запускает dev-сервер

    Проблема: На большом проекте (1000+ модулей) — минуты ожидания
    ```

    **Vite — использует нативные ES Modules:**
    ```
    1. Запускает dev-сервер СРАЗУ
    2. Браузер запрашивает index.html
    3. HTML ссылается на main.ts как ES Module
    4. Браузер запрашивает main.ts
    5. Vite трансформирует ТОЛЬКО запрошенный файл
    6. Браузер запрашивает импорты из main.ts
    7. Vite трансформирует их по требованию

    Итог: Трансформируется только то, что реально нужно
    ```

    **Визуальное сравнение:**

    ```
    Webpack (Bundle-based):
    ┌─────────────────────────────────────────────┐
    │  Bundle ALL modules → Start server → Ready  │
    └─────────────────────────────────────────────┘
    |←────── Долго (секунды/минуты) ──────────→|

    Vite (ESM-based):
    ┌──────────────┐
    │ Start server │ → Ready!
    └──────────────┘
    |← Мгновенно →|

    Трансформация происходит по запросу браузера
    ```

    **Как это работает технически:**

    ```html
    <!-- index.html -->
    <script type="module" src="/src/main.ts"></script>
    ```

    ```typescript
    // Браузер делает запрос: GET /src/main.ts
    // Vite отвечает трансформированным кодом:

    // Было:
    import { createApp } from 'vue'
    import App from './App.vue'

    // Стало (что получает браузер):
    import { createApp } from '/@modules/vue.js'  // Переписанный путь
    import App from '/src/App.vue?t=123456'       // С timestamp для HMR
    ```

    **Pre-bundling зависимостей:**
    Vite всё же бандлит `node_modules` при первом запуске (через esbuild):
    - CommonJS → ESM конвертация
    - Много мелких файлов → один файл (lodash = 600 модулей)
    - Кэшируется в `node_modules/.vite`

    **Hot Module Replacement (HMR):**
    ```
    Webpack: Перебандлить затронутые chunks → отправить браузеру
    Vite: Отправить только изменённый модуль по WebSocket

    Результат: HMR в Vite почти мгновенный, независимо от размера проекта
    ```

    **Production build:**
    Оба используют bundling (Vite использует Rollup), потому что:
    - HTTP/2 не так эффективен для 1000+ запросов
    - Tree shaking, минификация, code splitting

    **Когда что использовать:**
    - **Vite** — новые проекты, Vue/React/Svelte, быстрая разработка
    - **Webpack** — legacy проекты, сложные кастомные конфигурации, специфичные плагины

30. **SSR (Next.js)**: Server Components vs Client Components. Когда использовать `use client`?

    **Ответ:**

    **Server Components (по умолчанию в Next.js App Router):**
    - Рендерятся **только на сервере**
    - Не попадают в JavaScript-бандл клиента
    - Могут напрямую обращаться к базе данных, файлам, API
    - Не могут использовать useState, useEffect, браузерные API

    ```typescript
    // app/users/page.tsx — Server Component по умолчанию
    async function UsersPage() {
      // Прямой запрос к БД — это безопасно, код не попадёт в браузер
      const users = await db.query('SELECT * FROM users');

      return (
        <ul>
          {users.map(user => <li key={user.id}>{user.name}</li>)}
        </ul>
      );
    }
    ```

    **Client Components:**
    - Рендерятся на сервере (SSR) И в браузере (hydration)
    - Могут использовать React хуки и браузерные API
    - Добавляют JavaScript в бандл клиента

    ```typescript
    'use client';  // Обязательная директива в начале файла

    import { useState } from 'react';

    function Counter() {
      const [count, setCount] = useState(0);

      return (
        <button onClick={() => setCount(count + 1)}>
          Clicked {count} times
        </button>
      );
    }
    ```

    **Когда использовать `use client`:**

    | Нужен `use client` | Server Component подойдёт |
    |--------------------|---------------------------|
    | useState, useEffect | Получение данных (fetch, db) |
    | onClick, onChange | Статический контент |
    | Браузерные API (localStorage, window) | Рендеринг markdown |
    | Сторонние библиотеки с хуками | Работа с файловой системой |
    | Real-time updates | SEO-критичный контент |

    **Паттерн: Server Component + Client Component:**

    ```typescript
    // app/posts/page.tsx — Server Component
    import { LikeButton } from './LikeButton';

    async function PostsPage() {
      const posts = await fetchPosts();  // На сервере

      return (
        <div>
          {posts.map(post => (
            <article key={post.id}>
              <h2>{post.title}</h2>
              <p>{post.content}</p>
              {/* Интерактивная часть — Client Component */}
              <LikeButton postId={post.id} initialLikes={post.likes} />
            </article>
          ))}
        </div>
      );
    }
    ```

    ```typescript
    // app/posts/LikeButton.tsx — Client Component
    'use client';

    import { useState } from 'react';

    export function LikeButton({ postId, initialLikes }: Props) {
      const [likes, setLikes] = useState(initialLikes);

      const handleLike = async () => {
        setLikes(likes + 1);  // Оптимистичное обновление
        await fetch(`/api/posts/${postId}/like`, { method: 'POST' });
      };

      return <button onClick={handleLike}>❤️ {likes}</button>;
    }
    ```

    **Важные правила:**

    1. **Server Components могут импортировать Client Components** ✅
    2. **Client Components НЕ могут импортировать Server Components напрямую** ❌
       - Но могут принимать их как children:

    ```typescript
    'use client';

    function ClientWrapper({ children }: { children: React.ReactNode }) {
      const [isOpen, setIsOpen] = useState(false);
      return isOpen ? children : null;  // children может быть Server Component
    }
    ```

    **Преимущества Server Components:**
    - Меньше JavaScript на клиенте → быстрее загрузка
    - Прямой доступ к backend-ресурсам
    - Секретные ключи и токены не утекают в браузер
    - Лучший SEO (контент сразу в HTML)

    **Практический совет:**
    Начинайте с Server Components, добавляйте `use client` только когда нужна интерактивность. "Поднимайте" `use client` как можно ниже по дереву компонентов.

## 6. Testing (Advanced)

### 🟢 Базовые

31. **Типы тестов**: Unit, Integration, E2E. Что и когда тестировать?
32. **Snapshot Testing**: Плюсы и минусы. Почему снепшоты часто становятся бесполезными?

### 🟡 Продвинутые

33. **Mocking**: Как мокать API запросы (MSW, Jest mocks)? Почему лучше мокать на уровне сети, а не реализации функции?
34. **Testing Library**: Почему `getByRole` лучше, чем `getByTestId` или поиск по селекторам? (Принцип "тестируй как пользователь").
