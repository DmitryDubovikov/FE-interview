# Python Backend — Краткая Шпаргалка

Быстрое повторение перед интервью. Подробные ответы в `python_backend_interview_questions.md`.

---

## 🟢 Python и Фреймворки

**1. GIL** — глобальная блокировка в CPython, позволяющая выполнять только один поток. Для CPU-bound задач используй `multiprocessing`, для I/O-bound — `asyncio` или threads.

**2. asyncio** — event loop управляет корутинами, переключаясь при `await`. Эффективнее threads для тысяч одновременных I/O операций (нет overhead на создание потоков).

**3. Django vs FastAPI** — Django для full-stack с админкой и ORM, FastAPI для чистых API с нативным async и автодокументацией.

**4. N+1 проблема** — ORM делает N дополнительных запросов для связанных объектов. Решение: `select_related` (FK) и `prefetch_related` (M2M).

**5. FastAPI Pydantic/DI** — Pydantic автоматически валидирует request/response по type hints. `Depends()` инжектит зависимости (db, auth) и управляет их lifecycle.

---

## 🟢 SQL и Базы данных

**6. Индексы** — B-Tree ускоряет поиск до O(log n). Создавай для колонок в WHERE/JOIN/ORDER BY с высокой кардинальностью. `EXPLAIN ANALYZE` показывает план запроса.

**7. Уровни изоляции** — Read Committed (дефолт PostgreSQL) защищает от dirty reads. Serializable — полная изоляция, но медленнее. ACID = Atomicity, Consistency, Isolation, Durability.

**8. Aurora vs MySQL** — Aurora быстрее (5x write), автомасштабируется, 6 копий в 3 AZ, failover <30 сек. MySQL дешевле для простых приложений.

**9. Оптимизация запросов** — добавить индексы, убрать `SELECT *`, не использовать функции на индексированных колонках, проверить N+1, денормализация или кеш.

---

## 🟡 Key-Value и Кеширование

**10. Redis структуры** — String (кеш), Hash (объекты), List (очереди), Set (уникальные), Sorted Set (лидерборды), Stream (события).

**11. DynamoDB** — Partition Key определяет шард (должен быть высококардинальным), Sort Key для range queries. GSI — альтернативный индекс с другим PK.

**12. Стратегии кеширования** — Cache-Aside: читаем кеш → miss → БД → пишем в кеш. Write-Through: пишем в БД и кеш одновременно. Write-Behind: пишем только в кеш, асинхронно в БД.

**13. Cache Invalidation** — TTL (простота, но данные устаревают), явное удаление при изменении, event-based инвалидация. Комбинируй TTL + явную инвалидацию.

---

## 🟡 AWS Services

**14. Lambda** — холодный старт при первом вызове (уменьшай package, инициализацию вне handler). Лимиты: 15 мин timeout, 10 GB RAM, 6 MB payload sync.

**15. SQS/SNS/EventBridge** — SQS = очередь (pull, retention 14д), SNS = pub/sub (push, fan-out), EventBridge = event bus с фильтрацией и интеграциями.

**16. API Gateway + Lambda** — Lambda Proxy передаёт весь request в Lambda. HTTP API дешевле и проще, REST API больше features (WebSocket, кеш).

**17. Step Functions** — оркестрация workflows с визуализацией, retry, error handling. Для long-running процессов и координации сервисов. Не для real-time <1 сек.

---

## 🟡 gRPC и Микросервисы

**18. gRPC vs REST** — gRPC быстрее (HTTP/2, Protobuf), поддерживает streaming, строгий контракт. REST проще, работает в браузере, лучше для публичных API.

**19. Sync vs Async** — Sync (REST/gRPC) для немедленного ответа. Async (очереди/события) для слабой связанности, отказоустойчивости, долгих операций. Saga для распределённых транзакций.

**20. Circuit Breaker** — при частых ошибках "размыкает" цепь и отклоняет запросы, давая сервису восстановиться. + Retry с exponential backoff, timeout < timeout сервера.

**21. Service Mesh** — sidecar proxy (Envoy) перехватывает трафик между сервисами. Даёт mTLS, traffic management, observability без изменения кода. Istio, Linkerd.

---

## 🟠 BigData

**22. ETL vs ELT** — ETL трансформирует до загрузки (on-premise). ELT загружает сырые данные, трансформирует в DWH (cloud-native: Snowflake, BigQuery). Современный стек: Fivetran + dbt.

**23. Batch vs Streaming** — Batch для высокого throughput с задержкой (Spark). Streaming для real-time с низкой latency (Kafka, Flink). Lambda arch = оба параллельно.

**24. Data Lake vs DWH** — Lake хранит сырые данные дёшево (S3), schema-on-read. DWH — структурированные данные, быстрый SQL. Lakehouse = Lake + транзакции (Delta Lake).

---

## 🟠 Алгоритмы

**25. Big O** — O(1) dict/array доступ, O(log n) бинарный поиск, O(n) проход, O(n log n) сортировка, O(n²) вложенные циклы. Оптимизация: hash table вместо вложенного поиска.

**26. Hash Table** — O(1) доступ через hash(key) → index. Коллизии решаются chaining или open addressing. Load factor ~0.66 → resize. В Python: dict, set.

**27. Выбор структуры** — Array для доступа по индексу, Hash для поиска по ключу, Tree для сортированных данных и range queries, Heap для min/max и priority queue.

---

## 🟠 Patterns и Security

**28. SOLID** — S: одна ответственность, O: открыт для расширения, L: подклассы заменяемы, I: специализированные интерфейсы, D: зависеть от абстракций.

**29. Паттерны** — Factory создаёт объекты без указания класса. Singleton — один экземпляр (config, logger). Strategy — выбор алгоритма в runtime через интерфейс.

**30. Security** — SQL Injection: параметризованные запросы. XSS: экранирование (автоматическое в шаблонизаторах). OWASP: access control, HTTPS, обновление зависимостей, валидация входных данных.

---

## 🟡 Payments Processing

**31. Идемпотентность платежей** — клиент передаёт Idempotency-Key (UUID), сервер сохраняет результат и при повторном запросе возвращает его. Защищает от двойного списания при сетевых ошибках.

**32. PCI DSS** — стандарт безопасности для работы с картами. Главное: никогда не хранить CVV, шифровать PAN, токенизация. Проще всего — использовать Stripe/Adyen, чтобы данные карт не касались твоего сервера.

**33. Saga Pattern** — для распределённых транзакций вместо 2PC. Последовательность локальных транзакций с компенсирующими действиями при ошибке: CreateOrder → ReserveInventory → ProcessPayment, при ошибке — RefundPayment → ReleaseInventory.

---

## 🟡 Risk Management & Compliance

**34. Fraud Detection** — Rule-based (velocity, amount threshold, geo anomaly) + ML (anomaly detection). Важен баланс Precision/Recall: слишком строго = теряем клиентов, слишком мягко = теряем деньги.

**35. KYC/AML** — KYC: верификация личности (документ + liveness check + санкционные списки). AML: мониторинг транзакций на подозрительные паттерны (структурирование, необычные суммы). Данные хранить 5-7 лет.

---

## 🟡 System Integration

**36. Паттерны интеграции** — API (sync, немедленный ответ), Events (async, слабая связанность), ETL (batch, большие объёмы). Выбор зависит от требований к latency и consistency.

**37. Legacy и API versioning** — Anti-Corruption Layer изолирует legacy от нового кода через адаптер. API versioning: URL path (/v1/, /v2/) — самый простой. Deprecation policy: предупреждать за 6-12 месяцев.

**38. Retry и ошибки** — Retryable (timeout, 503) vs Non-retryable (400, 401). Exponential backoff для retry. Dead Letter Queue для failed messages. Circuit Breaker защищает от каскадных отказов.

---

## Быстрые ответы на частые вопросы

| Вопрос | Ответ |
|--------|-------|
| Как ускорить CPU-bound? | `multiprocessing` |
| Как ускорить I/O-bound? | `asyncio` или threads |
| Django или FastAPI? | Django = full-stack, FastAPI = API + async |
| Когда Redis? | Кеш, сессии, очереди, real-time |
| Когда DynamoDB? | Serverless, автомасштабирование, key-value |
| SQS или SNS? | SQS = очередь, SNS = broadcast |
| gRPC или REST? | gRPC внутри, REST наружу |
| Sync или async микросервисы? | Sync для ответа, async для надёжности |
| ETL или ELT? | ETL legacy, ELT cloud |
| Batch или Stream? | Batch отчёты, Stream real-time |
| Как избежать двойного списания? | Idempotency Key |
| PCI DSS кратко? | Не хранить CVV, токенизация, использовать Stripe |
| Saga vs 2PC? | Saga для микросервисов, 2PC блокирует |
| Fraud detection? | Rules + ML, баланс precision/recall |
| Как интегрировать legacy? | Anti-Corruption Layer (адаптер) |
