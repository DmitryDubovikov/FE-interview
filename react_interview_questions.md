# Топ-30 Вопросов для React Собеседования

Этот список составлен для подготовки к собеседованиям на позицию React Developer (Middle/Senior). Вопросы сгруппированы по уровню сложности и темам.

## 🟢 Разминка и База (Basic / Warm-up)

_Фундаментальные знания, без которых невозможно пройти собеседование._

1. **Что такое Virtual DOM и как он работает?**
   - _Ключевые слова:_ Reconciliation, Diffing algorithm, производительность.
2. **В чем разница между Element и Component в React?**
   - _Суть:_ Элемент — это простой объект (описание), компонент — это функция/класс (фабрика).
3. **Зачем нужен атрибут `key` при рендеринге списков?**
   - _Суть:_ Идентификация элементов при изменениях. Почему нельзя использовать index массива как ключ (проблемы при сортировке/удалении).
4. **Props vs State: В чем ключевые отличия?**
   - _Суть:_ Неизменяемость пропсов (read-only), локальность стейта.
5. **Жизненный цикл компонента (Lifecycle).**
   - _Суть:_ Mounting, Updating, Unmounting. Аналогия методов класса (`componentDidMount` и др.) с хуком `useEffect`.
6. **Что такое JSX?**
   - _Суть:_ Синтаксический сахар над `React.createElement`. Браузер не понимает JSX, нужен транспилятор (Babel).
7. **Управляемые (Controlled) и Неуправляемые (Uncontrolled) компоненты.**
   - _Суть:_ Кто управляет значением формы — React state или DOM. Использование `ref` для неуправляемых.
8. **Что такое `React.Fragment` и зачем он нужен?**
   - _Суть:_ Группировка элементов без добавления лишнего узла в DOM (`<>...</>`).
9. **Почему нельзя менять стейт напрямую (`this.state.x = ...`)?**
   - _Суть:_ React не узнает об изменении и не запустит перерисовку (re-render).
10. **Что такое "поднятие стейта" (Lifting State Up)?**

- _Суть:_ Передача данных от родителя к детям и обратно через колбэки для синхронизации.

## 🟡 Хуки и Паттерны (Intermediate)

_Основной инструмент современной разработки. Ожидается глубокое понимание._

11. **`useEffect`: Массив зависимостей и жизненный цикл.**
    - _Суть:_ Когда эффект запускается? Как сделать "cleanup"? Что будет, если не указать зависимости?
12. **`useState`: Батчинг (Batching) обновлений.**
    - _Суть:_ Почему несколько вызовов `setCount` подряд не обновляют стейт мгновенно? (React 18 automatic batching).
13. **`useRef`: Два основных юзкейса.**
    - _Суть:_ 1. Доступ к DOM-элементам. 2. Хранение мутабельных значений, которые переживают ререндер, но не вызывают его.
14. **`useMemo` и `useCallback`: Когда использовать?**
    - _Суть:_ Мемоизация вычислений и ссылочная целостность функций. Почему не стоит оборачивать всё подряд (оверхед).
15. **Custom Hooks (Пользовательские хуки).**
    - _Суть:_ Зачем нужны? (Переиспользование логики, а не UI). Правило именования `use...`.
16. **Context API vs Redux/Zustand.**
    - _Суть:_ Когда достаточно Контекста? Проблема "Prop Drilling". Проблема лишних ререндеров при обновлении контекста.
17. **`useLayoutEffect` vs `useEffect`.**
    - _Суть:_ Синхронность vs Асинхронность. Блокировка браузера. Использование для замеров DOM до отрисовки.
18. **Правила Хуков (Rules of Hooks).**
    - _Суть:_ Только на верхнем уровне (не в циклах/условиях), только в React-функциях. Почему важен порядок вызовов?
19. **`useReducer` vs `useState`.**
    - _Суть:_ Когда стейт сложный или зависит от предыдущего. Аналогия с Redux.
20. **Error Boundaries (Предохранители).**
    - _Суть:_ Классовые компоненты с `componentDidCatch`. Ловят ли ошибки в хуках/асинхронщине? (Нет).

## 🟠 Продвинутые темы и Оптимизация (Advanced)

_Вопросы, отличающие опытного разработчика._

21. **React.memo и PureComponent.**
    - _Суть:_ Shallow comparison (поверхностное сравнение) пропсов. Как предотвратить лишние ререндеры.
22. **Code Splitting и Lazy Loading.**
    - _Суть:_ `React.lazy`, `Suspense`. Разделение бандла для ускорения начальной загрузки.
23. **Порталы (Portals).**
    - _Суть:_ Рендеринг дочернего элемента в другой узел DOM (вне иерархии родителя). Примеры: модалки, тултипы.
24. **Проблема замыканий в хуках (Stale Closures).**
    - _Суть:_ Почему внутри `useEffect` или `setTimeout` переменная стейта может быть старой? Как лечить (`useRef` или добавление в зависимости).
25. **Synthetic Events (Синтетические события).**
    - _Суть:_ Кроссбраузерная обертка. Event Delegation (один слушатель на root).
26. **HOC (Higher-Order Components) и Render Props.**
    - _Суть:_ Паттерны переиспользования логики до хуков. Плюсы и минусы (Wrapper Hell).
27. **SSR (Server-Side Rendering) vs CSR.**
    - _Суть:_ SEO, First Contentful Paint. Hydration (гидратация).
28. **React Fiber.**
    - _Суть:_ Новый движок согласования (reconciliation). Возможность прерывать и приоритезировать рендеринг.
29. **Тестирование React приложений.**
    - _Суть:_ Unit vs Integration. Jest + React Testing Library. Почему тестируем поведение, а не реализацию (snapshot testing vs user interactions).
30. **Безопасность в React (XSS).**
    - _Суть:_ Как React защищает от XSS (экранирование)? Опасность `dangerouslySetInnerHTML`.
