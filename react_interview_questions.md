# Топ-30 Вопросов для React Собеседования

Этот список составлен для подготовки к собеседованиям на позицию React Developer (Middle/Senior). Вопросы сгруппированы по уровню сложности и темам.

## 🟢 Разминка и База (Basic / Warm-up)

_Фундаментальные знания, без которых невозможно пройти собеседование._

1. **Что такое Virtual DOM и как он работает?**

   Virtual DOM (виртуальный DOM) — это легковесная копия реального DOM, которая хранится в памяти JavaScript. Это обычный JavaScript-объект, описывающий структуру UI.

   **Как это работает:**
   - При изменении состояния React создает новое дерево Virtual DOM
   - Затем запускается **Diffing algorithm** — сравнение нового дерева с предыдущим
   - React находит минимальный набор изменений (какие узлы добавить, удалить, обновить)
   - Только эти изменения применяются к реальному DOM

   **Зачем это нужно:** Работа с реальным DOM — дорогая операция (reflow, repaint). Virtual DOM позволяет «батчить» изменения и обновлять DOM минимальным количеством операций. Весь этот процесс называется **Reconciliation** (согласование).

---

2. **В чем разница между Element и Component в React?**

   **React Element** — это простой неизменяемый объект, который описывает, что должно появиться на экране. Это «снимок» UI в конкретный момент времени.

   ```jsx
   // Это элемент — просто объект
   const element = <h1>Hello</h1>;
   // Под капотом: { type: 'h1', props: { children: 'Hello' } }
   ```

   **React Component** — это функция или класс, который принимает props и возвращает React Element. Компонент — это «фабрика» элементов, он может содержать логику и состояние.

   ```jsx
   // Это компонент — функция, создающая элементы
   function Greeting({ name }) {
     return <h1>Hello, {name}</h1>;
   }
   ```

   **Аналогия:** Компонент — это чертеж дома, элемент — это конкретный дом, построенный по чертежу.

---

3. **Зачем нужен атрибут `key` при рендеринге списков?**

   `key` — это специальный атрибут, который помогает React идентифицировать элементы в списке и понимать, какие из них изменились, были добавлены или удалены.

   ```jsx
   {users.map(user => (
     <UserCard key={user.id} user={user} />
   ))}
   ```

   **Почему нельзя использовать index как key:**
   - При сортировке, фильтрации или добавлении элементов в начало списка индексы смещаются
   - React «думает», что элементы изменились, хотя поменялся только порядок
   - Это приводит к: неправильному обновлению состояния компонентов, потере фокуса в инпутах, проблемам с анимациями

   **Хороший key:** уникальный и стабильный идентификатор (id из базы данных, uuid). Индекс допустим только для статических списков, которые не меняются.

---

4. **Props vs State: В чем ключевые отличия?**

   | Props | State |
   |-------|-------|
   | Передаются от родителя | Создаются внутри компонента |
   | Read-only (неизменяемы) | Можно изменять через setState/useState |
   | Компонент не контролирует свои props | Компонент полностью контролирует свой state |
   | Изменение props родителем вызывает ререндер | Изменение state вызывает ререндер |

   **Props** — это входные данные компонента, как аргументы функции. Компонент не должен их модифицировать.

   **State** — это внутреннее состояние компонента, его «память». Изменение state через `setState` или `useState` сообщает React, что нужно перерисовать компонент.

   ```jsx
   function Counter({ initialValue }) {  // initialValue — это prop
     const [count, setCount] = useState(initialValue);  // count — это state
     return <button onClick={() => setCount(count + 1)}>{count}</button>;
   }
   ```

---

5. **Жизненный цикл компонента (Lifecycle)**

   Каждый компонент проходит три основные фазы:

   **1. Mounting (монтирование)** — компонент создается и вставляется в DOM
   - Классы: `constructor` → `render` → `componentDidMount`
   - Хуки: `useState` инициализация → рендер → `useEffect(() => {}, [])`

   **2. Updating (обновление)** — компонент перерисовывается из-за изменения props или state
   - Классы: `render` → `componentDidUpdate`
   - Хуки: рендер → `useEffect` с зависимостями

   **3. Unmounting (размонтирование)** — компонент удаляется из DOM
   - Классы: `componentWillUnmount`
   - Хуки: функция очистки из `useEffect`

   ```jsx
   useEffect(() => {
     // componentDidMount + componentDidUpdate
     const subscription = subscribe();

     return () => {
       // componentWillUnmount (cleanup)
       subscription.unsubscribe();
     };
   }, [dependency]);
   ```

---

6. **Что такое JSX?**

   JSX (JavaScript XML) — это расширение синтаксиса JavaScript, позволяющее писать HTML-подобный код внутри JavaScript. Это **синтаксический сахар** над вызовами `React.createElement()`.

   ```jsx
   // Это JSX:
   const element = <h1 className="title">Hello, {name}!</h1>;

   // Компилируется в:
   const element = React.createElement('h1', { className: 'title' }, `Hello, ${name}!`);
   ```

   **Важные моменты:**
   - Браузер не понимает JSX — нужен транспилятор (Babel)
   - В JSX используется `className` вместо `class` (зарезервированное слово в JS)
   - Можно вставлять любые JS-выражения в фигурных скобках `{}`
   - Компоненты должны начинаться с заглавной буквы (иначе React решит, что это HTML-тег)

---

7. **Управляемые (Controlled) и Неуправляемые (Uncontrolled) компоненты**

   **Controlled (управляемый)** — значение формы хранится в React state. React полностью контролирует элемент.

   ```jsx
   function ControlledInput() {
     const [value, setValue] = useState('');
     return (
       <input
         value={value}
         onChange={(e) => setValue(e.target.value)}
       />
     );
   }
   ```

   **Uncontrolled (неуправляемый)** — значение хранится в самом DOM. Для доступа используется `ref`.

   ```jsx
   function UncontrolledInput() {
     const inputRef = useRef(null);
     const handleSubmit = () => {
       console.log(inputRef.current.value);
     };
     return <input ref={inputRef} defaultValue="initial" />;
   }
   ```

   **Когда что использовать:**
   - Controlled: когда нужна валидация на лету, условное отключение кнопки, форматирование ввода
   - Uncontrolled: простые формы, интеграция с non-React кодом, работа с файлами (`<input type="file">`)

---

8. **Что такое `React.Fragment` и зачем он нужен?**

   `React.Fragment` позволяет группировать несколько элементов без добавления лишнего узла в DOM.

   **Проблема:** React-компонент должен возвращать один корневой элемент. Раньше приходилось оборачивать в `<div>`, что засоряло DOM.

   ```jsx
   // Плохо — лишний div в DOM
   return (
     <div>
       <Header />
       <Main />
     </div>
   );

   // Хорошо — Fragment не создает узел в DOM
   return (
     <React.Fragment>
       <Header />
       <Main />
     </React.Fragment>
   );

   // Короткий синтаксис
   return (
     <>
       <Header />
       <Main />
     </>
   );
   ```

   **Когда нужен полный синтаксис:** при рендеринге списков, чтобы указать `key`:
   ```jsx
   {items.map(item => (
     <React.Fragment key={item.id}>
       <dt>{item.term}</dt>
       <dd>{item.description}</dd>
     </React.Fragment>
   ))}
   ```

---

9. **Почему нельзя менять стейт напрямую (`this.state.x = ...`)?**

   При прямом изменении state React не узнает, что данные изменились, и **не запустит перерисовку** компонента.

   ```jsx
   // НЕПРАВИЛЬНО — React не узнает об изменении
   this.state.count = this.state.count + 1;

   // ПРАВИЛЬНО — React запланирует ререндер
   this.setState({ count: this.state.count + 1 });
   ```

   **Почему так устроено:**
   - React оптимизирует обновления, группируя несколько `setState` в одно обновление (batching)
   - React сравнивает предыдущий и новый state для определения необходимости ререндера
   - Прямая мутация нарушает принцип иммутабельности, на котором построен React

   **То же правило для хуков:**
   ```jsx
   const [items, setItems] = useState([]);

   // НЕПРАВИЛЬНО
   items.push(newItem);

   // ПРАВИЛЬНО
   setItems([...items, newItem]);
   ```

---

10. **Что такое "поднятие стейта" (Lifting State Up)?**

    Это паттерн, когда общий state перемещается в ближайший общий компонент-предок, чтобы несколько компонентов могли его использовать и синхронизироваться.

    **Проблема:** Два компонента-соседа должны использовать одни и те же данные, но state локален для каждого компонента.

    **Решение:** «Поднять» state в родителя и передавать данные и колбэки через props.

    ```jsx
    // Родитель владеет state
    function Parent() {
      const [value, setValue] = useState('');

      return (
        <>
          <InputComponent value={value} onChange={setValue} />
          <DisplayComponent value={value} />
        </>
      );
    }

    // Дети получают данные через props
    function InputComponent({ value, onChange }) {
      return <input value={value} onChange={(e) => onChange(e.target.value)} />;
    }

    function DisplayComponent({ value }) {
      return <p>Вы ввели: {value}</p>;
    }
    ```

    **Принцип:** данные текут сверху вниз (props), события — снизу вверх (callbacks). Это называется «однонаправленный поток данных» (one-way data flow).

## 🟡 Хуки и Паттерны (Intermediate)

_Основной инструмент современной разработки. Ожидается глубокое понимание._

11. **`useEffect`: Массив зависимостей и жизненный цикл.**

    `useEffect` — это хук для выполнения побочных эффектов: запросы к API, подписки, манипуляции с DOM.

    **Когда запускается эффект:**
    ```jsx
    // 1. После КАЖДОГО рендера (обычно не нужно)
    useEffect(() => {
      console.log('Рендер произошел');
    });

    // 2. Только при монтировании (пустой массив)
    useEffect(() => {
      fetchData();
    }, []);

    // 3. При изменении конкретных зависимостей
    useEffect(() => {
      fetchUser(userId);
    }, [userId]);
    ```

    **Cleanup (очистка):** Функция, возвращаемая из `useEffect`, вызывается перед следующим запуском эффекта и при размонтировании компонента.

    ```jsx
    useEffect(() => {
      const socket = new WebSocket(url);

      return () => {
        socket.close(); // Очистка — закрываем соединение
      };
    }, [url]);
    ```

    **Что будет без массива зависимостей:** Эффект будет запускаться после каждого рендера. Если внутри есть `setState`, получится бесконечный цикл: рендер → эффект → setState → рендер → ...

    **Важно:** React сравнивает зависимости через `Object.is()`. Объекты и массивы сравниваются по ссылке, поэтому `[1,2,3] !== [1,2,3]`. Это частая причина лишних запусков эффекта.

---
12. **`useState`: Батчинг (Batching) обновлений.**

    **Батчинг** — это оптимизация React, при которой несколько обновлений состояния группируются в один ререндер.

    ```jsx
    function Counter() {
      const [count, setCount] = useState(0);

      const handleClick = () => {
        setCount(count + 1);  // count всё ещё 0
        setCount(count + 1);  // count всё ещё 0
        setCount(count + 1);  // count всё ещё 0
        // Результат: count станет 1, а не 3!
      };
    }
    ```

    **Почему так происходит:** `setCount` не меняет `count` мгновенно. Он «планирует» обновление. Все три вызова читают одно и то же значение `count = 0` из замыкания.

    **Решение — функциональная форма:**
    ```jsx
    const handleClick = () => {
      setCount(prev => prev + 1);  // 0 → 1
      setCount(prev => prev + 1);  // 1 → 2
      setCount(prev => prev + 1);  // 2 → 3
      // Результат: count станет 3
    };
    ```

    **React 18 Automatic Batching:** До React 18 батчинг работал только в обработчиках событий React. Теперь он работает везде: в `setTimeout`, `Promise.then()`, нативных событиях. Это улучшает производительность без изменения кода.

    ```jsx
    // React 18: один ререндер вместо двух
    setTimeout(() => {
      setCount(c => c + 1);
      setFlag(f => !f);
      // Раньше: 2 ререндера. Теперь: 1 ререндер
    }, 1000);
    ```

---
13. **`useRef`: Два основных юзкейса.**

    `useRef` возвращает мутабельный объект `{ current: initialValue }`, который сохраняется между рендерами.

    **Юзкейс 1: Доступ к DOM-элементам**
    ```jsx
    function TextInput() {
      const inputRef = useRef(null);

      const focusInput = () => {
        inputRef.current.focus(); // Прямой доступ к DOM-элементу
      };

      return (
        <>
          <input ref={inputRef} />
          <button onClick={focusInput}>Фокус на инпут</button>
        </>
      );
    }
    ```

    **Юзкейс 2: Хранение мутабельных значений без ререндера**
    ```jsx
    function Timer() {
      const [count, setCount] = useState(0);
      const intervalRef = useRef(null);

      const start = () => {
        intervalRef.current = setInterval(() => {
          setCount(c => c + 1);
        }, 1000);
      };

      const stop = () => {
        clearInterval(intervalRef.current); // ID интервала сохранился
      };

      return <div>{count}</div>;
    }
    ```

    **Ключевое отличие от `useState`:**
    - Изменение `ref.current` **не вызывает** ререндер
    - Значение доступно **сразу** после изменения (без ожидания следующего рендера)
    - `ref` — это как «карман» компонента, куда можно положить что угодно

    **Типичные применения:** хранение предыдущего значения props/state, ID таймеров, флаги для предотвращения двойного вызова.

---
14. **`useMemo` и `useCallback`: Когда использовать?**

    Оба хука — инструменты мемоизации, но для разных целей.

    **`useMemo`** — мемоизирует **результат вычисления**:
    ```jsx
    function ProductList({ products, filter }) {
      // Тяжелая фильтрация выполнится только при изменении products или filter
      const filteredProducts = useMemo(() => {
        return products.filter(p => p.category === filter);
      }, [products, filter]);

      return <List items={filteredProducts} />;
    }
    ```

    **`useCallback`** — мемоизирует **саму функцию** (её ссылку):
    ```jsx
    function Parent() {
      const [count, setCount] = useState(0);

      // Без useCallback: новая функция при каждом рендере
      // С useCallback: та же ссылка, пока не изменятся зависимости
      const handleClick = useCallback(() => {
        console.log('Clicked');
      }, []);

      return <MemoizedChild onClick={handleClick} />;
    }
    ```

    **Когда использовать:**
    - `useMemo`: дорогие вычисления (сортировка больших массивов, сложные расчеты)
    - `useCallback`: передача колбэков в мемоизированные дочерние компоненты (`React.memo`)

    **Почему НЕ стоит использовать везде:**
    ```jsx
    // Плохо — оверхед больше, чем польза
    const name = useMemo(() => user.firstName + ' ' + user.lastName, [user]);

    // Хорошо — просто вычисление
    const name = user.firstName + ' ' + user.lastName;
    ```

    Мемоизация имеет свою цену: хранение предыдущего значения в памяти, сравнение зависимостей. Для простых операций это дороже, чем пересчитать заново.

    **Правило:** Сначала напиши код без мемоизации. Добавляй `useMemo`/`useCallback` только когда есть измеримая проблема с производительностью.

---
15. **Custom Hooks (Пользовательские хуки).**

    Custom Hook — это функция, начинающаяся с `use`, которая использует другие хуки. Это способ **переиспользовать логику** между компонентами.

    **Пример: хук для работы с localStorage**
    ```jsx
    function useLocalStorage(key, initialValue) {
      const [value, setValue] = useState(() => {
        const saved = localStorage.getItem(key);
        return saved ? JSON.parse(saved) : initialValue;
      });

      useEffect(() => {
        localStorage.setItem(key, JSON.stringify(value));
      }, [key, value]);

      return [value, setValue];
    }

    // Использование в разных компонентах
    function Settings() {
      const [theme, setTheme] = useLocalStorage('theme', 'light');
      const [language, setLanguage] = useLocalStorage('lang', 'ru');
      // ...
    }
    ```

    **Пример: хук для fetch-запросов**
    ```jsx
    function useFetch(url) {
      const [data, setData] = useState(null);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState(null);

      useEffect(() => {
        setLoading(true);
        fetch(url)
          .then(res => res.json())
          .then(setData)
          .catch(setError)
          .finally(() => setLoading(false));
      }, [url]);

      return { data, loading, error };
    }

    // Использование
    function UserProfile({ userId }) {
      const { data: user, loading, error } = useFetch(`/api/users/${userId}`);
      if (loading) return <Spinner />;
      if (error) return <Error />;
      return <Profile user={user} />;
    }
    ```

    **Почему имя должно начинаться с `use`:**
    - ESLint-плагин проверяет правила хуков только для функций с `use`
    - Это соглашение сообщает разработчикам, что функция может использовать хуки внутри

    **Важно:** Каждый вызов хука создает **изолированное** состояние. Если два компонента используют `useLocalStorage('theme')`, у каждого будет своя копия состояния (хотя данные в localStorage общие).

---
16. **Context API vs Redux/Zustand.**

    **Context API** — встроенный механизм React для передачи данных через дерево компонентов без «пробрасывания» props на каждом уровне.

    **Prop Drilling — проблема, которую решает Context:**
    ```jsx
    // Без Context: props передаются через 5 уровней
    <App user={user}>
      <Layout user={user}>
        <Sidebar user={user}>
          <UserMenu user={user}>
            <Avatar user={user} />  // Только здесь нужен user!
          </UserMenu>
        </Sidebar>
      </Layout>
    </App>

    // С Context: данные доступны напрямую
    const UserContext = createContext(null);

    function App() {
      const [user, setUser] = useState(null);
      return (
        <UserContext.Provider value={user}>
          <Layout />  {/* Не нужно передавать user */}
        </UserContext.Provider>
      );
    }

    function Avatar() {
      const user = useContext(UserContext);  // Получаем напрямую
      return <img src={user.avatar} />;
    }
    ```

    **Проблема Context — лишние ререндеры:**
    При изменении значения Context **все** потребители перерисовываются, даже если им нужна только часть данных.

    ```jsx
    // Плохо: изменение theme перерисует компоненты, использующие только user
    <AppContext.Provider value={{ user, theme, settings }}>
    ```

    **Когда Context достаточно:**
    - Редко меняющиеся данные (тема, локаль, текущий пользователь)
    - Небольшое приложение
    - Данные нужны многим компонентам

    **Когда нужен Redux/Zustand:**
    - Частые обновления состояния
    - Сложная логика обновлений
    - Нужна нормализация данных
    - Важна производительность (выборочные подписки)

    **Zustand** — легковесная альтернатива Redux с простым API:
    ```jsx
    const useStore = create((set) => ({
      count: 0,
      increment: () => set((state) => ({ count: state.count + 1 })),
    }));

    function Counter() {
      const count = useStore((state) => state.count); // Подписка только на count
      return <div>{count}</div>;
    }
    ```

---
17. **`useLayoutEffect` vs `useEffect`.**

    Оба хука выполняют побочные эффекты, но в разные моменты.

    **Порядок выполнения:**
    ```
    1. React вычисляет изменения (render phase)
    2. React обновляет DOM
    3. useLayoutEffect выполняется СИНХРОННО ← блокирует отрисовку
    4. Браузер отрисовывает экран (paint)
    5. useEffect выполняется АСИНХРОННО ← после отрисовки
    ```

    **`useEffect`** (99% случаев):
    - Выполняется асинхронно, после отрисовки
    - Не блокирует визуальное обновление
    - Подходит для: запросов к API, подписок, логирования

    **`useLayoutEffect`** (редкие случаи):
    - Выполняется синхронно, до отрисовки
    - Блокирует браузер — пользователь ждет
    - Подходит для: измерения DOM, предотвращения «мерцания»

    **Пример — когда нужен `useLayoutEffect`:**
    ```jsx
    function Tooltip({ targetRef }) {
      const [position, setPosition] = useState({ top: 0, left: 0 });

      // useEffect: тултип сначала появится в (0,0), потом прыгнет — мерцание!
      // useLayoutEffect: позиция вычислится ДО отрисовки — без мерцания
      useLayoutEffect(() => {
        const rect = targetRef.current.getBoundingClientRect();
        setPosition({ top: rect.bottom, left: rect.left });
      }, [targetRef]);

      return <div style={{ position: 'absolute', ...position }}>Tooltip</div>;
    }
    ```

    **Правило:** Используй `useEffect` по умолчанию. Переходи на `useLayoutEffect` только если видишь визуальное мерцание или нужны синхронные измерения DOM.

---
18. **Правила Хуков (Rules of Hooks).**

    Два главных правила, нарушение которых ломает работу хуков:

    **Правило 1: Вызывай хуки только на верхнем уровне**

    Нельзя вызывать хуки внутри циклов, условий или вложенных функций.

    ```jsx
    // НЕПРАВИЛЬНО
    function Form({ isAdmin }) {
      if (isAdmin) {
        const [role, setRole] = useState('admin'); // Условный вызов!
      }
      const [name, setName] = useState('');
    }

    // ПРАВИЛЬНО
    function Form({ isAdmin }) {
      const [role, setRole] = useState(isAdmin ? 'admin' : 'user');
      const [name, setName] = useState('');
    }
    ```

    **Почему это важно:** React идентифицирует хуки по порядку их вызова. Если порядок изменится между рендерами, React «перепутает» какой state к какому хуку относится.

    ```jsx
    // Первый рендер (isAdmin = true):
    useState('admin')  // hook #1 → role
    useState('')       // hook #2 → name

    // Второй рендер (isAdmin = false):
    // useState('admin') пропущен!
    useState('')       // hook #1 → React думает это role!
    // Всё сломалось
    ```

    **Правило 2: Вызывай хуки только в React-функциях**

    Хуки работают только в:
    - Функциональных компонентах React
    - Кастомных хуках (функции, начинающиеся с `use`)

    ```jsx
    // НЕПРАВИЛЬНО — обычная функция
    function calculateTotal(items) {
      const [total, setTotal] = useState(0); // Ошибка!
    }

    // ПРАВИЛЬНО — кастомный хук
    function useTotal(items) {
      const [total, setTotal] = useState(0);
      // ...
      return total;
    }
    ```

    **Инструменты:** ESLint-плагин `eslint-plugin-react-hooks` автоматически проверяет эти правила.

---
19. **`useReducer` vs `useState`.**

    `useReducer` — альтернатива `useState` для сложной логики состояния. Работает по принципу Redux: `(state, action) => newState`.

    **Синтаксис:**
    ```jsx
    const [state, dispatch] = useReducer(reducer, initialState);

    function reducer(state, action) {
      switch (action.type) {
        case 'increment':
          return { count: state.count + 1 };
        case 'decrement':
          return { count: state.count - 1 };
        case 'reset':
          return { count: action.payload };
        default:
          return state;
      }
    }

    function Counter() {
      const [state, dispatch] = useReducer(reducer, { count: 0 });

      return (
        <>
          <span>{state.count}</span>
          <button onClick={() => dispatch({ type: 'increment' })}>+</button>
          <button onClick={() => dispatch({ type: 'decrement' })}>-</button>
          <button onClick={() => dispatch({ type: 'reset', payload: 0 })}>Reset</button>
        </>
      );
    }
    ```

    **Когда использовать `useReducer`:**
    - Состояние — объект с несколькими связанными полями
    - Следующее состояние зависит от предыдущего
    - Много разных способов обновления состояния
    - Логика обновления сложная и её хочется вынести отдельно

    **Когда достаточно `useState`:**
    - Простые значения (число, строка, boolean)
    - Независимые части состояния
    - Простая логика обновления

    **Пример — форма с несколькими полями:**
    ```jsx
    // С useState — много отдельных состояний
    const [name, setName] = useState('');
    const [email, setEmail] = useState('');
    const [errors, setErrors] = useState({});
    const [isSubmitting, setIsSubmitting] = useState(false);

    // С useReducer — всё в одном месте, логика централизована
    const [formState, dispatch] = useReducer(formReducer, {
      values: { name: '', email: '' },
      errors: {},
      isSubmitting: false
    });
    ```

    **Бонус:** reducer — чистая функция, её легко тестировать отдельно от компонента.

---
20. **Error Boundaries (Предохранители).**

    Error Boundary — это компонент, который перехватывает JavaScript-ошибки в дочернем дереве компонентов и показывает запасной UI вместо «белого экрана смерти».

    **Реализация (только классовый компонент!):**
    ```jsx
    class ErrorBoundary extends React.Component {
      state = { hasError: false, error: null };

      // Вызывается при ошибке — обновляем state для показа fallback UI
      static getDerivedStateFromError(error) {
        return { hasError: true, error };
      }

      // Вызывается после ошибки — для логирования
      componentDidCatch(error, errorInfo) {
        console.error('Error caught:', error);
        logErrorToService(error, errorInfo.componentStack);
      }

      render() {
        if (this.state.hasError) {
          return <h1>Что-то пошло не так</h1>;
        }
        return this.props.children;
      }
    }

    // Использование
    <ErrorBoundary>
      <MyWidget />  {/* Если здесь ошибка — покажется fallback */}
    </ErrorBoundary>
    ```

    **Что Error Boundary НЕ ловит:**
    - Ошибки в обработчиках событий (`onClick`, `onChange`)
    - Асинхронный код (`setTimeout`, `fetch`, `Promise`)
    - Серверный рендеринг (SSR)
    - Ошибки в самом Error Boundary

    ```jsx
    function Button() {
      const handleClick = () => {
        throw new Error('Клик!'); // Error Boundary НЕ поймает это!
      };
      return <button onClick={handleClick}>Click</button>;
    }
    ```

    **Для обработчиков событий используй try-catch:**
    ```jsx
    const handleClick = () => {
      try {
        doSomethingRisky();
      } catch (error) {
        setError(error); // Обработка через state
      }
    };
    ```

    **Почему нет хука для Error Boundary?** Пока React не предоставляет хук-версию. Но можно использовать библиотеку `react-error-boundary`, которая предоставляет удобный API:

    ```jsx
    import { ErrorBoundary } from 'react-error-boundary';

    <ErrorBoundary fallback={<ErrorFallback />}>
      <MyComponent />
    </ErrorBoundary>
    ```

    **Совет:** Размещай Error Boundaries стратегически — вокруг крупных секций приложения, чтобы ошибка в одном виджете не ломала всю страницу.

## 🟠 Продвинутые темы и Оптимизация (Advanced)

_Вопросы, отличающие опытного разработчика._

21. **React.memo и PureComponent.**

    Оба инструмента предотвращают лишние ререндеры через **поверхностное сравнение** (shallow comparison) props.

    **React.memo** — HOC для функциональных компонентов:
    ```jsx
    const MovieCard = React.memo(function MovieCard({ title, rating }) {
      console.log('MovieCard render');
      return <div>{title}: {rating}★</div>;
    });

    // Родитель перерендерился, но если title и rating не изменились —
    // MovieCard НЕ перерисуется
    ```

    **PureComponent** — то же самое для классовых компонентов:
    ```jsx
    class MovieCard extends React.PureComponent {
      render() {
        return <div>{this.props.title}</div>;
      }
    }
    ```

    **Что такое Shallow Comparison:**
    - Примитивы (string, number, boolean) сравниваются по значению
    - Объекты и массивы сравниваются **по ссылке**

    ```jsx
    // Проблема: объект создается заново при каждом рендере родителя
    function Parent() {
      return <MovieCard style={{ color: 'red' }} />; // Новый объект каждый раз!
    }

    // Решение: вынести объект или использовать useMemo
    const style = { color: 'red' };
    function Parent() {
      return <MovieCard style={style} />; // Та же ссылка
    }
    ```

    **Кастомная функция сравнения:**
    ```jsx
    const MovieCard = React.memo(
      function MovieCard({ movie }) {
        return <div>{movie.title}</div>;
      },
      (prevProps, nextProps) => {
        // Вернуть true = НЕ перерендеривать
        return prevProps.movie.id === nextProps.movie.id;
      }
    );
    ```

    **Когда НЕ использовать memo:**
    - Компонент и так рендерится редко
    - Props почти всегда разные
    - Компонент очень простой (сравнение дороже рендера)

---
22. **Code Splitting и Lazy Loading.**

    **Code Splitting** — разделение JavaScript-бандла на части, которые загружаются по требованию. Это ускоряет начальную загрузку приложения.

    **React.lazy** — функция для динамического импорта компонентов:
    ```jsx
    // Вместо обычного импорта
    // import HeavyChart from './HeavyChart';

    // Ленивый импорт — компонент загрузится только когда понадобится
    const HeavyChart = React.lazy(() => import('./HeavyChart'));
    ```

    **Suspense** — компонент-обёртка, показывающий fallback пока ленивый компонент загружается:
    ```jsx
    function Dashboard() {
      return (
        <div>
          <h1>Dashboard</h1>
          <Suspense fallback={<Spinner />}>
            <HeavyChart />  {/* Пока грузится — показываем Spinner */}
          </Suspense>
        </div>
      );
    }
    ```

    **Типичные сценарии использования:**

    ```jsx
    // 1. Разделение по роутам — самый частый случай
    const Home = lazy(() => import('./pages/Home'));
    const Profile = lazy(() => import('./pages/Profile'));
    const Settings = lazy(() => import('./pages/Settings'));

    function App() {
      return (
        <Suspense fallback={<PageLoader />}>
          <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/profile" element={<Profile />} />
            <Route path="/settings" element={<Settings />} />
          </Routes>
        </Suspense>
      );
    }

    // 2. Тяжёлые компоненты, которые не всегда нужны
    const PdfViewer = lazy(() => import('./PdfViewer'));
    const CodeEditor = lazy(() => import('./CodeEditor'));

    // 3. Модальные окна
    const SettingsModal = lazy(() => import('./SettingsModal'));
    ```

    **Как это работает под капотом:**
    - Webpack/Vite создают отдельные chunk-файлы для lazy-компонентов
    - При первом рендере компонента браузер загружает соответствующий chunk
    - После загрузки компонент кэшируется и больше не загружается

    **Именованные экспорты:**
    ```jsx
    // Если компонент экспортируется не по default
    const MyComponent = lazy(() =>
      import('./MyModule').then(module => ({ default: module.MyComponent }))
    );
    ```

---
23. **Порталы (Portals).**

    Portal позволяет отрендерить дочерний элемент в DOM-узел, который находится **вне** иерархии родительского компонента.

    **Синтаксис:**
    ```jsx
    import { createPortal } from 'react-dom';

    function Modal({ children, isOpen }) {
      if (!isOpen) return null;

      return createPortal(
        <div className="modal-overlay">
          <div className="modal-content">
            {children}
          </div>
        </div>,
        document.getElementById('modal-root')  // Рендерим сюда, а не в родителя
      );
    }
    ```

    ```html
    <!-- index.html -->
    <body>
      <div id="root"></div>        <!-- Основное приложение -->
      <div id="modal-root"></div>  <!-- Сюда рендерятся модалки -->
    </body>
    ```

    **Зачем это нужно:**

    1. **CSS-изоляция** — модалка не наследует `overflow: hidden`, `z-index`, `transform` от родителей
    ```jsx
    // Проблема: модалка обрезается из-за overflow: hidden родителя
    <div style={{ overflow: 'hidden' }}>
      <Modal>Я обрезан!</Modal>  // Без портала
    </div>

    // Решение: портал рендерит модалку вне этого div
    ```

    2. **Семантика и доступность** — модалки и тултипы логически должны быть на верхнем уровне DOM

    **Важно:** Несмотря на то, что элемент рендерится в другом месте DOM, он остаётся частью React-дерева:
    - События всплывают через React-иерархию (не DOM!)
    - Context работает как обычно
    - Родительский компонент может управлять порталом

    ```jsx
    function Parent() {
      const [count, setCount] = useState(0);

      return (
        <div onClick={() => setCount(c => c + 1)}>
          <p>Кликов: {count}</p>
          {/* Клик внутри модалки ВСПЛЫВЁТ к Parent через React! */}
          <Modal isOpen={true}>
            <button>Клик увеличит count</button>
          </Modal>
        </div>
      );
    }
    ```

    **Типичные случаи использования:**
    - Модальные окна
    - Тултипы и поповеры
    - Уведомления (toasts)
    - Выпадающие меню, которые должны выходить за границы контейнера

---
24. **Проблема замыканий в хуках (Stale Closures).**

    **Stale Closure** (устаревшее замыкание) — ситуация, когда функция «захватила» старое значение переменной и не видит её обновлений.

    **Проблема в действии:**
    ```jsx
    function Counter() {
      const [count, setCount] = useState(0);

      useEffect(() => {
        const id = setInterval(() => {
          console.log(count);  // Всегда 0! Замыкание захватило начальное значение
          setCount(count + 1); // Всегда устанавливает 1
        }, 1000);

        return () => clearInterval(id);
      }, []);  // Пустой массив — эффект создан один раз с count = 0

      return <div>{count}</div>;  // Показывает 1, 1, 1, 1...
    }
    ```

    **Почему это происходит:**
    - При первом рендере `count = 0`
    - `useEffect` создаёт интервал с функцией, которая «замкнула» `count = 0`
    - Массив зависимостей пуст — эффект не пересоздаётся
    - Функция интервала навсегда видит `count = 0`

    **Решение 1: Функциональное обновление state**
    ```jsx
    useEffect(() => {
      const id = setInterval(() => {
        setCount(prev => prev + 1);  // Не зависит от замыкания!
      }, 1000);
      return () => clearInterval(id);
    }, []);
    ```

    **Решение 2: Добавить зависимость (пересоздавать эффект)**
    ```jsx
    useEffect(() => {
      const id = setInterval(() => {
        console.log(count);  // Теперь актуальное значение
        setCount(count + 1);
      }, 1000);
      return () => clearInterval(id);
    }, [count]);  // Эффект пересоздаётся при каждом изменении count
    ```

    **Решение 3: useRef для актуального значения**
    ```jsx
    function Counter() {
      const [count, setCount] = useState(0);
      const countRef = useRef(count);

      // Синхронизируем ref с актуальным state
      useEffect(() => {
        countRef.current = count;
      }, [count]);

      useEffect(() => {
        const id = setInterval(() => {
          console.log(countRef.current);  // Всегда актуальное значение!
        }, 1000);
        return () => clearInterval(id);
      }, []);

      return <div>{count}</div>;
    }
    ```

    **Где чаще всего встречается:**
    - `setTimeout` / `setInterval` внутри `useEffect`
    - Обработчики событий, созданные в `useEffect`
    - Колбэки, переданные во внешние библиотеки

    **Правило:** Если функция использует значение из state/props и живёт дольше одного рендера — задумайтесь о stale closure.

---
25. **Synthetic Events (Синтетические события).**

    **SyntheticEvent** — это кроссбраузерная обёртка React над нативными событиями браузера. Вы работаете с ней, когда используете `onClick`, `onChange` и другие обработчики.

    ```jsx
    function Button() {
      const handleClick = (event) => {
        // event — это SyntheticEvent, не нативный Event
        console.log(event.type);        // 'click'
        console.log(event.target);      // DOM-элемент
        console.log(event.nativeEvent); // Оригинальный нативный event
      };

      return <button onClick={handleClick}>Click me</button>;
    }
    ```

    **Зачем нужна обёртка:**
    - **Кроссбраузерность** — одинаковое API во всех браузерах
    - **Производительность** — React использует Event Delegation

    **Event Delegation (делегирование событий):**

    React не вешает обработчик на каждый элемент. Вместо этого:
    - Один слушатель на корневом элементе (root)
    - Когда событие всплывает, React определяет какой компонент его вызвал
    - Вызывает соответствующий обработчик

    ```jsx
    // Вы пишете так:
    <ul>
      {items.map(item => (
        <li key={item.id} onClick={() => handleClick(item.id)}>
          {item.name}
        </li>
      ))}
    </ul>

    // React НЕ создаёт 1000 обработчиков для 1000 элементов.
    // Один обработчик на root перехватывает все клики.
    ```

    **Пулинг событий (до React 17):**

    В старых версиях React объекты событий переиспользовались (pooling). После обработки свойства обнулялись:

    ```jsx
    // React 16 и раньше — проблема
    const handleClick = (event) => {
      setTimeout(() => {
        console.log(event.type); // null! Событие уже в пуле
      }, 100);
    };

    // Решение — сохранить нужные данные или вызвать event.persist()
    ```

    **React 17+:** Пулинг убрали. События больше не переиспользуются, `event.persist()` не нужен.

    **Отличия от нативных событий:**
    - `onChange` в React срабатывает при каждом изменении (в нативном — только при blur)
    - Имена событий в camelCase: `onClick`, `onMouseEnter`
    - Обработчик получает SyntheticEvent, а не нативный Event

    **Когда нужен нативный event:**
    ```jsx
    const handleClick = (syntheticEvent) => {
      const nativeEvent = syntheticEvent.nativeEvent;
      // Для специфичных браузерных API
    };
    ```

---
26. **HOC (Higher-Order Components) и Render Props.**

    Это паттерны переиспользования логики между компонентами, популярные до появления хуков.

    **HOC (Higher-Order Component)** — функция, которая принимает компонент и возвращает новый компонент с расширенной функциональностью.

    ```jsx
    // HOC добавляет данные о пользователе
    function withUser(WrappedComponent) {
      return function EnhancedComponent(props) {
        const [user, setUser] = useState(null);

        useEffect(() => {
          fetchCurrentUser().then(setUser);
        }, []);

        if (!user) return <Spinner />;

        return <WrappedComponent {...props} user={user} />;
      };
    }

    // Использование
    function Profile({ user }) {
      return <div>Hello, {user.name}</div>;
    }

    const ProfileWithUser = withUser(Profile);
    // <ProfileWithUser /> — автоматически получит user
    ```

    **Render Props** — паттерн, где компонент получает функцию через props и вызывает её для рендеринга.

    ```jsx
    // Компонент с render prop
    function Mouse({ render }) {
      const [position, setPosition] = useState({ x: 0, y: 0 });

      useEffect(() => {
        const handleMove = (e) => setPosition({ x: e.clientX, y: e.clientY });
        window.addEventListener('mousemove', handleMove);
        return () => window.removeEventListener('mousemove', handleMove);
      }, []);

      return render(position);  // Вызываем функцию с данными
    }

    // Использование
    <Mouse render={({ x, y }) => (
      <div>Курсор: {x}, {y}</div>
    )} />

    // Или через children
    <Mouse>
      {({ x, y }) => <div>Курсор: {x}, {y}</div>}
    </Mouse>
    ```

    **Проблема: Wrapper Hell**

    При использовании нескольких HOC/Render Props код становится нечитаемым:

    ```jsx
    // HOC Wrapper Hell
    export default withRouter(
      withTheme(
        withUser(
          withAnalytics(MyComponent)
        )
      )
    );

    // Render Props Wrapper Hell
    <ThemeProvider>
      {theme => (
        <UserProvider>
          {user => (
            <AnalyticsProvider>
              {analytics => (
                <MyComponent theme={theme} user={user} analytics={analytics} />
              )}
            </AnalyticsProvider>
          )}
        </UserProvider>
      )}
    </ThemeProvider>
    ```

    **Решение — хуки:**
    ```jsx
    function MyComponent() {
      const theme = useTheme();
      const user = useUser();
      const analytics = useAnalytics();
      const router = useRouter();

      // Плоская структура, легко читается
    }
    ```

    **Когда HOC/Render Props всё ещё актуальны:**
    - Работа с легаси-кодом
    - Классовые компоненты (хуки там не работают)
    - Некоторые библиотеки используют эти паттерны (react-router v5, styled-components)

---
27. **SSR (Server-Side Rendering) vs CSR.**

    **CSR (Client-Side Rendering)** — браузер скачивает пустой HTML + JS, затем JavaScript рендерит всё приложение.

    ```html
    <!-- Что получает браузер при CSR -->
    <html>
      <body>
        <div id="root"></div>  <!-- Пусто! -->
        <script src="bundle.js"></script>  <!-- 500KB+ -->
      </body>
    </html>
    ```

    **SSR (Server-Side Rendering)** — сервер генерирует готовый HTML, браузер показывает контент сразу.

    ```html
    <!-- Что получает браузер при SSR -->
    <html>
      <body>
        <div id="root">
          <header>...</header>
          <main>Контент уже здесь!</main>  <!-- Готовый HTML -->
        </div>
        <script src="bundle.js"></script>
      </body>
    </html>
    ```

    **Сравнение:**

    | Критерий | CSR | SSR |
    |----------|-----|-----|
    | First Contentful Paint | Медленный (ждём JS) | Быстрый |
    | SEO | Плохое (боты видят пустую страницу) | Хорошее |
    | Time to Interactive | Быстрее | Медленнее (hydration) |
    | Нагрузка на сервер | Минимальная | Высокая |
    | Сложность | Простая | Сложная |

    **Hydration (гидратация):**

    После SSR браузер получает статичный HTML. Чтобы сделать его интерактивным, React «оживляет» его — привязывает обработчики событий и state. Этот процесс называется гидратацией.

    ```jsx
    // На сервере
    import { renderToString } from 'react-dom/server';
    const html = renderToString(<App />);

    // На клиенте — гидратация
    import { hydrateRoot } from 'react-dom/client';
    hydrateRoot(document.getElementById('root'), <App />);
    ```

    **Проблема гидратации:**
    - HTML виден сразу, но кнопки не работают пока не загрузится JS
    - Пользователь может кликнуть на кнопку, а она не реагирует
    - React 18 решает это через Selective Hydration (приоритетная гидратация интерактивных элементов)

    **Современные подходы:**
    - **SSG (Static Site Generation)** — HTML генерируется при сборке, не при каждом запросе
    - **ISR (Incremental Static Regeneration)** — статика + фоновое обновление
    - **Streaming SSR** — отправка HTML частями по мере готовности
    - **React Server Components** — компоненты рендерятся только на сервере

    **Фреймворки:** Next.js, Remix, Gatsby — упрощают работу с SSR/SSG.

---
28. **React Fiber.**

    **Fiber** — это архитектура согласования (reconciliation), появившаяся в React 16. Она заменила старый «Stack Reconciler» и дала React возможность прерывать и приоритезировать работу.

    **Проблема старого подхода (Stack Reconciler):**
    ```
    Рендер большого дерева компонентов
    ↓
    JavaScript заблокирован на 100мс
    ↓
    Браузер не может обработать ввод пользователя
    ↓
    Интерфейс «подвисает»
    ```

    Старый алгоритм работал синхронно — начав рендер, он должен был закончить его полностью, блокируя основной поток.

    **Что даёт Fiber:**

    1. **Прерываемый рендеринг** — работу можно остановить, сделать что-то важное, и продолжить
    2. **Приоритеты** — срочные обновления (ввод пользователя) важнее фоновых (загрузка данных)
    3. **Конкурентный режим (Concurrent Mode)** — несколько версий UI могут готовиться одновременно

    **Как это работает:**

    Fiber разбивает работу на маленькие единицы (units of work). Каждый компонент — это Fiber-узел с информацией:
    - Какой компонент рендерить
    - Какие props и state
    - Связи с родителем, детьми, соседями
    - Какую работу нужно сделать

    ```
    [Fiber 1] → [Fiber 2] → [Fiber 3] → пауза → обработка ввода → [Fiber 4] → ...
    ```

    **Практический результат — Concurrent Features в React 18:**

    ```jsx
    // useTransition — помечает обновление как «не срочное»
    function Search() {
      const [query, setQuery] = useState('');
      const [results, setResults] = useState([]);
      const [isPending, startTransition] = useTransition();

      const handleChange = (e) => {
        setQuery(e.target.value);  // Срочно — обновить инпут

        startTransition(() => {
          setResults(filterResults(e.target.value));  // Не срочно — можно прервать
        });
      };

      return (
        <>
          <input value={query} onChange={handleChange} />
          {isPending ? <Spinner /> : <ResultsList results={results} />}
        </>
      );
    }

    // useDeferredValue — отложенная версия значения
    function List({ query }) {
      const deferredQuery = useDeferredValue(query);
      // deferredQuery обновится когда браузер не занят
    }
    ```

    **Аналогия:** Старый React — как однозадачная ОС (DOS). Fiber — как многозадачная (Windows). Можно переключаться между задачами, не дожидаясь завершения каждой.

---
29. **Тестирование React приложений.**

    **Типы тестов:**

    | Тип | Что тестирует | Скорость | Уверенность |
    |-----|---------------|----------|-------------|
    | Unit | Отдельную функцию/хук | Очень быстро | Низкая |
    | Integration | Компонент + его зависимости | Быстро | Средняя |
    | E2E | Всё приложение в браузере | Медленно | Высокая |

    **Стек: Jest + React Testing Library**

    **Jest** — тест-раннер и assertion-библиотека.
    **React Testing Library (RTL)** — утилиты для рендеринга и взаимодействия с компонентами.

    ```jsx
    import { render, screen, fireEvent } from '@testing-library/react';
    import Counter from './Counter';

    test('увеличивает счётчик при клике', () => {
      // Рендерим компонент
      render(<Counter initialValue={0} />);

      // Находим элементы как пользователь (по тексту, роли, лейблу)
      const button = screen.getByRole('button', { name: /increment/i });
      const counter = screen.getByText('0');

      // Взаимодействуем
      fireEvent.click(button);

      // Проверяем результат
      expect(screen.getByText('1')).toBeInTheDocument();
    });
    ```

    **Философия RTL: Тестируй поведение, а не реализацию**

    ```jsx
    // ПЛОХО — тестируем детали реализации
    test('плохой тест', () => {
      const { container } = render(<Counter />);
      const button = container.querySelector('.increment-btn');  // CSS-класс
      expect(wrapper.state().count).toBe(0);  // Внутренний state
    });

    // ХОРОШО — тестируем с точки зрения пользователя
    test('хороший тест', () => {
      render(<Counter />);
      const button = screen.getByRole('button', { name: 'Increment' });
      fireEvent.click(button);
      expect(screen.getByText('Count: 1')).toBeInTheDocument();
    });
    ```

    **Почему плохо тестировать реализацию:**
    - Тесты ломаются при рефакторинге (поменял класс — тест упал)
    - Тесты не отражают реальное использование
    - Ложная уверенность — тест проходит, но компонент сломан

    **Snapshot Testing:**
    ```jsx
    test('snapshot', () => {
      const { asFragment } = render(<Button>Click me</Button>);
      expect(asFragment()).toMatchSnapshot();
    });
    ```

    Снэпшоты полезны для отслеживания неожиданных изменений, но имеют проблемы:
    - Легко бездумно обновить (`jest -u`)
    - Не говорят, что именно сломалось
    - Шум при небольших изменениях

    **Тестирование асинхронного кода:**
    ```jsx
    test('загружает данные', async () => {
      render(<UserProfile userId={1} />);

      // Ждём появления данных
      expect(await screen.findByText('John Doe')).toBeInTheDocument();

      // Или с waitFor для сложных условий
      await waitFor(() => {
        expect(screen.getByRole('heading')).toHaveTextContent('John Doe');
      });
    });
    ```

    **Мокирование:**
    ```jsx
    // Мок API-запроса
    jest.mock('./api', () => ({
      fetchUser: jest.fn(() => Promise.resolve({ name: 'John' }))
    }));
    ```

---
30. **Безопасность в React (XSS).**

    **XSS (Cross-Site Scripting)** — атака, при которой злоумышленник внедряет вредоносный JavaScript на страницу.

    **Как React защищает по умолчанию:**

    React автоматически экранирует все значения перед вставкой в DOM:

    ```jsx
    function Comment({ text }) {
      // Даже если text = "<script>alert('XSS')</script>"
      // React отобразит это как текст, а не выполнит как код
      return <div>{text}</div>;
    }

    // В DOM будет:
    // <div>&lt;script&gt;alert('XSS')&lt;/script&gt;</div>
    ```

    Символы `<`, `>`, `&`, `"`, `'` преобразуются в HTML-сущности, и браузер воспринимает их как текст, а не как код.

    **Опасность `dangerouslySetInnerHTML`:**

    Этот атрибут отключает экранирование и вставляет HTML напрямую:

    ```jsx
    // ОПАСНО — если userContent содержит скрипт, он выполнится!
    function Article({ userContent }) {
      return <div dangerouslySetInnerHTML={{ __html: userContent }} />;
    }

    // userContent = "<img src=x onerror='alert(document.cookie)'>"
    // Куки украдены!
    ```

    **Когда `dangerouslySetInnerHTML` нужен:**
    - Рендеринг HTML из CMS/редактора (Markdown → HTML)
    - Вставка виджетов третьих сторон
    - Работа с legacy-контентом

    **Как безопасно использовать:**

    ```jsx
    import DOMPurify from 'dompurify';

    function Article({ htmlContent }) {
      // Санитизация — удаляет опасные теги и атрибуты
      const cleanHTML = DOMPurify.sanitize(htmlContent);

      return <div dangerouslySetInnerHTML={{ __html: cleanHTML }} />;
    }
    ```

    **Другие векторы XSS в React:**

    ```jsx
    // 1. Опасные URL (javascript:)
    // ПЛОХО
    <a href={userProvidedUrl}>Link</a>  // userProvidedUrl = "javascript:alert('XSS')"

    // ХОРОШО — валидация URL
    const safeUrl = userProvidedUrl.startsWith('http') ? userProvidedUrl : '#';
    <a href={safeUrl}>Link</a>

    // 2. Inline стили (редко, но возможно)
    // В старых браузерах можно было: style="background: url('javascript:...')"
    // Современные браузеры это блокируют

    // 3. Серверный рендеринг
    // При SSR данные сериализуются в JSON — нужно экранировать
    <script>
      window.__INITIAL_STATE__ = {JSON.stringify(state).replace(/</g, '\\u003c')}
    </script>
    ```

    **Правила безопасности:**
    1. Никогда не используй `dangerouslySetInnerHTML` с пользовательским вводом без санитизации
    2. Валидируй URL перед вставкой в `href` или `src`
    3. Используй библиотеки санитизации (DOMPurify, sanitize-html)
    4. Content Security Policy (CSP) — дополнительный уровень защиты на сервере
